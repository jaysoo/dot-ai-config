# API Surface Audit — 2026-05

_Last updated: 2026-05-25_
_Scope: Nx CLI packages, devkit public API, astro-docs auto-generated reference, ocean publishable packages (best-effort via npm)_

## 2026-05-25 update (W22)

_Nx repo: `/tmp/nx-scan-1779715408` (shallow depth=1, HEAD `150a46be`, dated 2026-05-25). Window git history cross-referenced against `~/projects/nx`._
_Ocean repo: clone failed this run; verified via `npm view nx-cloud` — latest stable still `19.1.3` (2026-03-31), no new releases in window. Prior conclusions hold._
_Window: 2026-05-04 → 2026-05-25 (164 commits). Repo went **23.0.0-beta.4 → 23.0.0-beta.18** (14 betas shipped, including release-prep `beta.5/6/13/14` skipped in tag log)._

### Summary

| Severity | Count | Δ vs 2026-05-04 |
|----------|------:|----------------:|
| 🔴 High  | 1     | 0               |
| 🟠 Medium | 5    | **+1**          |
| 🟡 Low   | 4     | **+1**          |
| ℹ️ Info  | 2     | 0               |
| **Total actionable** | **10** | **+2** |

- `devkit-exports.ts`: 263 lines (was 261) — **+2** exported types this window (`TargetDefaultEntry`, `TargetDefaultsRecord` from PR #35340)
- `devkit-internals.ts`: 69 lines (was 49), **34 exports** (was 30) — gained `readTargetDefaultsForTarget` + `normalizeTargetDefaultsAgainstRootMaps` (#35340), `loadTsFile` + `forceRegisterEsmLoader` + `requireWithTsconfigFallback` (#35608 native TS strip), `emitPluginWorkerLog` (#35610 gradle plugin-v1 deprecation), `resolveImplementation` + `resolveSchema` + `ImplementationResolutionError` + `SchemaResolutionError` (schema-utils), `resolvePrompt` + `PromptResolutionError` (#35638 prompt field)
- `devkit-internals` consumer spread: **18 first-party packages** (was 11) — angular, cypress, detox, esbuild, eslint, expo, gradle, jest, js, maven, module-federation, next, react, react-native, rollup, rspack, webpack, devkit. Trendline worsened
- 4 prior carried-forward gaps **all still present**
- 1 new High-adjacent gap (catalogued as Medium): `nx watch --includeDependentProjects` legacy flag still in `reference/nx-commands.mdoc` example space, but watch guide already migrated to `--includeDependencies` (PR #35699). Deferred to Low — auto-generated yargs reference will pick it up

### Changes since prior section (2026-05-04 → 2026-05-25)

**Resolved this window:** None of the 4 carried items closed.

**Removed (favourable churn — confirms backlog is being worked):**
- `initTasksRunner` devkit API removed (#35708). `packages/nx/src/index.ts` is now an intentional empty stub with comment noting Nx Cloud's `require('nx/src/index')` probe needs the file
- `stripSourceCode` and non-native scanner removed (#35729) — clears one v22 deprecation past its window
- `replaceNrwlPackageWithNxPackage` devkit utility removed (#35679)
- TUI selection lifecycle helpers removed (#35649)
- Magic-string `'self'`/`'dependencies'` in `dependsOn.projects` **warns now**, scheduled for v24 drop (#35648 + #35687 follow-up). Source still accepts them but logs. Doc impact: see Low-1

**New surface added (4 items requiring doc updates, see Medium/Low sections):**
- `nx migrate --mode={first-party|third-party|all}` + `--multi-major-mode` flags (#35497) — **undocumented**
- `migrations.json` schema gains `prompt?: string` field for prompt-based migrations (#35638) — **undocumented in `extending-nx/migration-generators.mdoc`**
- `nx completion <shell>` command + tab-completion (#34951) — **only mentioned in env-vars page via `NX_COMPLETE`**; no dedicated reference page or how-to
- `TargetDefaultEntry`, `TargetDefaultsRecord` devkit types (#35340) — exported, no `@category`, no reference page. Auto-pickup via TypeDoc unverified

**New deprecations announced:**
- `@nx/gradle/plugin-v1` entry marked `@deprecated` on every public symbol + runtime warning on plugin load. Scheduled removal Nx 24 (#35610). One major lead time
- `nx watch --includeDependentProjects` deprecated in favor of `--includeDependencies`, alias kept till v24 (#35699). Watch guide already uses new name; nx-commands reference TBD via yargs auto-gen
- `@nx/cypress:cypress` executor `x-deprecated` annotation (#35531). Auto-doc pipeline picks up schema field; no prose drift

**Breaking removals shipped clean (auto-doc pipeline absorbed all):**
- `@nx/angular:ngrx` combined generator removed (#35567) — replaced by `ngrx-root-store` + `ngrx-feature-store` which are auto-documented via `generators.json`. No stale `@nx/angular:ngrx` references in `astro-docs/`
- SVGR option removed from `@nx/webpack`/`@nx/react` (#35611). One stale callout still claims "removed in Nx 22"/"deprecated for Rspack will be removed in Nx 23" — see Low-2
- `@nx/vite` vitest support removed (#35517). Migration guide intact; intro page correctly points users to `@nx/vitest`
- Legacy typescript plugin dropped + `rollup` `buildLibsFromSource` default aligned (#35516)
- `js` option dropped from component generators (#35616). `@deprecated js?: boolean` on `ArtifactGenerationOptions` still present in devkit at `packages/devkit/src/generators/artifact-name-and-directory-utils.ts:33` — see Medium below
- `releaseTag*` flat properties dropped from release config (#35694)
- Node 20 support dropped (#35591)
- Webpack plugin re-exports dropped + migration generated (#35659)

### 🔴 High — unchanged

#### packages/nx: `JsonInput` input type (`json`, `fields`, `excludeFields`)
**Status:** still drifting at HEAD `150a46be`. ~46 days since PR #35248 merged. `astro-docs/src/content/docs/reference/inputs.mdoc` headings remain `Project configuration / Command arguments / Source files / Environment variables / Runtime inputs / Working directory / External dependencies / Outputs of dependent tasks / A subset of the root tsconfig.json or tsconfig.base.json / Named inputs from other projects` — no `JSON` named-input section. `reference/nx-json.mdoc` likewise silent. Severity unchanged.

### 🟠 Medium — stale or mismatched

#### `nx migrate --mode` / `--multi-major-mode` undocumented (NEW)
- **Source:** `packages/nx/src/command-line/migrate/command-object.ts:93` registers `multiMajorMode`; PR #35497 (merged 2026-05-12)
- **Issue:** Two new flags + downgrade-prevention safeguard + bare-invocation default behaviour. No mention in `features/automate-updating-dependencies.mdoc`, `reference/releases.mdoc`, `getting-started/installation.mdoc`, or `reference/nx-commands.mdoc`. Only `reference/environment-variables.mdoc` has indirect reference
- **User impact:** Users on Nx 23 with multi-major jumps will hit the safety rail without knowing it exists, can't discover scoping flag for Nx-only migrations
- **Fix:** Add a "Migrate modes" subsection to `features/automate-updating-dependencies.mdoc`. Auto-gen yargs reference may or may not capture the flags depending on `withMigrateOptions` builder shape — verify by building docs

#### `migrations.json` `prompt` field undocumented (NEW)
- **Source:** `packages/nx/src/config/misc-interfaces.ts:89,102` add `prompt?: string` to migration entry shape; PR #35638 (merged 2026-05-14). Resolution helper `resolvePrompt` exported via `devkit-internals.ts`
- **Issue:** Plugin authors can now ship Markdown-only migrations without writing a generator. `astro-docs/src/content/docs/extending-nx/migration-generators.mdoc` has no `prompt` reference; users will keep writing boilerplate generators
- **Fix:** Add a "Prompt-only migrations" subsection alongside `implementation`/`factory`

#### `@nx/vite` `compiler` plugin option (carried, ~31 days)
- **Source:** `packages/vite/src/plugins/plugin.ts:51` — `compiler?: 'tsc' | 'tsgo' | 'vue-tsc'`
- **Status:** No change. PR #35429 merged 2026-04-24. `astro-docs/src/content/docs/technologies/build-tools/vite/introduction.mdoc` still has no plugin-options block

#### `skipProjectGraph` flag undocumented and unregistered (carried, ~46 days)
- **Source:** `packages/nx/src/command-line/generate/generate.ts:315,433` reads `args.skipProjectGraph`; not in `withGenerateOptions`
- **Status:** No change. PR #35170 merged 2026-04-09. Flag still hidden from yargs `--help`

#### `@nx/devkit` deprecated APIs retained past stated removal version (carried, escalating)
- `addPluginV1` (`packages/devkit/src/utils/add-plugin.ts:62`) — `@deprecated Use 'addPlugin' instead`. Still exported
- `getDependencyVersionFromPackageJson(Tree, …)` overloads (`packages/devkit/src/utils/package-json.ts:108-115`) — `@deprecated Use the other function signature without a Tree`. Both `tree, packageName` overloads still exported
- `js?: boolean` + `jsOptionName?: string` on `ArtifactGenerationOptions` (`packages/devkit/src/generators/artifact-name-and-directory-utils.ts:33,37`) — `@deprecated … will be removed in Nx v21`. Repo now on **23.0.0-beta.18**, **2 majors overdue**, **3rd audit cycle reported**. Note that #35616 (drop `js` option from component generators) removed user-facing surface but did NOT remove the devkit type members

#### `devkit-internals` usage spread widened (NEW — worsening trendline)
- **Consumers grew 11 → 18 first-party packages** this window: now angular, cypress, detox, esbuild, eslint, expo, gradle, jest, js, maven, module-federation, next, react, react-native, rollup, rspack, webpack, devkit
- **Export count grew 30 → 34 symbols** (~13% growth in 21 days)
- **Why it grew:** Native TS strip work (#35608) added 3 register helpers; target-defaults filtering (#35340) added 2; gradle plugin-v1 deprecation (#35610) added `emitPluginWorkerLog`; prompt field (#35638) added `resolvePrompt` + error type; schema-utils added 4 helpers
- **Issue:** "Internal" surface is de facto public within the monorepo with no stability contract or doc page. Blast radius for any signature change is now 18 packages
- **Fix:** Stability-tier note in devkit reference, or a contract-test set covering this surface. Same recommendation as 3 prior cycles

### 🟡 Low — Missing JSDoc / minor

| Package | Symbol / Issue | Notes |
|---------|----------------|-------|
| `@nx/devkit` | 9 ungrouped exports in `devkit-exports.ts` (was 7) | Lines 43, 45, 64, 69, 165, 249, 261 carried; `TargetDefaultEntry` + `TargetDefaultsRecord` (new from #35340) also lack `@category`. Cosmetic for TypeDoc |
| `@nx/js` | `nx.sync.ignoredDependencies` | No change since 2026-04. Still absent from `concepts/sync-generators.mdoc` and `reference/nx-json.mdoc`; sibling `ignoredReferences` also undocumented |
| `@nx/next` | `withNx` `svgr` option doc stale | `astro-docs/src/content/docs/technologies/react/next/Guides/next-config-setup.mdoc:44` reads "Deprecated: … This option will be removed in Nx 22." Repo on 23.0.0-beta.18; option has been removed. Likewise `adding-assets-react.mdoc:48` says "deprecated for Rspack (will be removed in Nx 23)." — that removal shipped (#35611). Both notes need updating to past-tense |
| `nx` | `nx completion <shell>` command undocumented (NEW) | PR #34951 (merged in window) adds shell tab-completion for bash/zsh/fish/powershell with scripts at `packages/nx/src/command-line/completion/scripts/`. Only mention in `astro-docs/` is the internal `NX_COMPLETE` env var on env-vars page. No reference entry, no installation snippet, no how-to. Lower than Medium because the feature is discoverable via `nx --help` once shipped |

### ℹ️ Info — Internal API leaks (unchanged)

- **`devkit-internals` consumers:** 34 symbols, 18 consumer packages (was 30 / 11). See Medium item — escalation candidate
- **Ocean `nx-cloud` public surface:** `npm view nx-cloud version` still `19.1.3` (2026-03-31). No new releases in window. `libs/nx-packages/nx-cloud/index.ts` shape from prior cycle stands. **Ocean clone failed this run** — orchestrator-level issue, not audit-level

### Doc gaps tracking (prior 4 + 4 new)

| # | Source PR | Merged | Doc gap | Status W22 | Days drifting |
|---|-----------|--------|---------|------------|---------------|
| 1 | #35248 (JsonInput) | 2026-04-10 | `reference/inputs.mdoc` missing json named-input section | **OPEN** | ~46 |
| 2 | #35429 (vite compiler) | 2026-04-24 | `technologies/build-tools/vite/introduction.mdoc` missing plugin-options | **OPEN** | ~31 |
| 3 | #35401 (sync.ignoredDependencies) | 2026-04-23 | `concepts/sync-generators.mdoc` + `reference/nx-json.mdoc` silent | **OPEN** | ~32 |
| 4 | #35170 (skipProjectGraph) | 2026-04-09 | yargs `withGenerateOptions` missing flag; not in `reference/nx-commands.mdoc` | **OPEN** | ~46 |
| 5 | #35497 (migrate --mode) | 2026-05-12 | `features/automate-updating-dependencies.mdoc` missing modes | **NEW** | ~13 |
| 6 | #35638 (migrations.json prompt) | 2026-05-14 | `extending-nx/migration-generators.mdoc` silent on prompt field | **NEW** | ~11 |
| 7 | #34951 (tab completion) | in window | No reference page or how-to for `nx completion` | **NEW** | ~20 |
| 8 | #35340 (TargetDefaultEntry/Record) | 2026-05-15 | New devkit exports lack `@category` | **NEW (Low)** | ~10 |

### Methodology Notes — W22

- **Data freshness:** HEAD `150a46be` (shallow depth=1). Per-PR attribution and ~14-beta sequence reconstructed from `~/projects/nx` git log (depth-1 scan clone can't supply window history alone)
- **PR-state verification:** Done via commit-SHA presence in user-repo `git log` (`gh pr view` still flaky — not retried this cycle)
- **Ocean fallback:** Same as prior — clone fails at orchestrator level; covered via `npm view nx-cloud time --json`. **Recommend escalating Ocean clone fix to orchestrator** since this is the 3rd consecutive cycle with the same gap
- **Auto-doc coverage:** Verified for breaking removals (ngrx, vitest, SVGR usability, legacy typescript plugin, webpack re-exports, releaseTag, Node 20). The auto-pipeline absorbed every executor/generator-schema change cleanly. The drift is concentrated in (a) prose-only docs for new CLI flags and (b) devkit type exports lacking JSDoc categories
- **Confidence:** High for HEAD state. Lookback windowing relied on user-repo log (164 commits classified). Window length (21 days, ~3 weeks) is the longest in any audit cycle so far — explains the +2 actionable items even though no carried items closed

---

## 2026-05-04 baseline section

_Last updated: 2026-05-04_
_Scope: Nx CLI packages, devkit public API, astro-docs auto-generated reference, ocean publishable packages (best-effort via npm)_
_Nx repo: `/tmp/nx-scan-1777915188` (shallow clone, HEAD `0f5b8f4`, dated 2026-05-04). Cross-referenced against `~/projects/nx` for git history (shallow clone is depth=1)._
_Ocean repo: clone failed this run; covered via `npm view nx-cloud` (latest `19.1.3`, no new stable since 2026-03-31)._

## Summary

| Severity | Count | Δ vs 2026-04 |
|----------|------:|-------------:|
| 🔴 High  | 1     | 0            |
| 🟠 Medium | 4    | 0            |
| 🟡 Low   | 3     | 0            |
| ℹ️ Info  | 2     | 0            |
| **Total actionable** | **8** | **0** |

- Public exports checked: `devkit-exports.ts` (261 lines, unchanged), `devkit-internals.ts` (49 lines, unchanged)
- Nx packages with generators/executors at HEAD: 33 generator packages + 28 executor packages
- `plugin-mappings.ts`: 477 lines (37 entries via `pluginToTechnology`); all generator-bearing packages mapped (`nx`, `web`, `workspace` are intentional non-plugin exclusions)
- New stable nx-cloud release this window: **none** (19.1.3 since 2026-03-31; ocean public TS surface unchanged)
- Repo shipped 23.0.0-beta.2/3/4 in this window — v21-marked devkit deprecations are now **2 majors past stated removal**, unchanged severity

## Changes Since Last Run (2026-04-28 → 2026-05-04)

### New drift introduced this window
- **None.** No new public-surface additions whose docs were skipped. The auto-doc pipeline (`generators.json`/`executors.json` → `plugin-schema-parser.ts`) absorbed all schema-affecting changes (e.g. detox `x-deprecated` annotation in PR #35529).

### Resolved this window
- **None.** All 2026-04 items still present at HEAD `0f5b8f4`.

### New surface-relevant commits (verified, no doc drift introduced)
- **PR #35518** `fix(core): restore deprecated allWorkspaceFiles on WorkspaceFileMap` (2026-04-30) — re-adds `allWorkspaceFiles` as `@deprecated` non-enumerable getter to repair `@nx/owners` consumer (`createFileMapUsingProjectGraph` deep-import via `nx/src/project-graph/file-map-utils`). Field is on `WorkspaceFileMap` interface (not in `devkit-exports.ts`); ships with explicit `@deprecated` JSDoc. **No drift** — restoration with proper deprecation cycle.
- **PR #35529** `feat(detox)!: deprecate the @nx/detox build and test executors` (2026-05-02) — adds `x-deprecated` schema annotation pointing users to `convert-to-inferred`. Auto-doc pipeline picks this up; `astro-docs/src/content/docs/technologies/test-tools/detox/` pages have no stale executor refs in prose. **No drift.**
- **PR #35513** `feat(angular)!: remove deprecated move generator` (2026-04-30) — `@nx/angular:move` removed (was deprecated v18). Zero references in `astro-docs/`. **No drift.**
- **PR #35512** `feat(angular)!: remove deprecated @nx/angular/module-federation entry point` (2026-04-30) — entry point removed (deprecated v20.2). Zero references in `astro-docs/`; module-federation docs already point at `@nx/module-federation/angular`. Migration `update-23-0-0-update-with-module-federation-import` ships. **No drift.**
- **PR #35103** `feat(misc)!: remove deprecated stylesheet options from generators` (2026-04-28) — Less / Tailwind / CSS-in-JS options removed from React/Next/Vue/Nuxt/Web/Workspace generators. No stale `--style=less` / `--style=styled-components` / `--style=emotion` references in `astro-docs/`. Schema removals propagate via auto-doc. **No drift.**
- **PR #35367** `feat(core): show target uses task graph + filter broken dependsOn during normalization` (2026-04-30) — `nx show target` output gains transitive task list. CLI behavior change, no public API surface added; reference docs auto-derive from yargs.
- **PR #34946** `chore(devkit): build devkit to local dist and use nodenext` (2026-05-01) — internal build target relocation; consumers resolve via standard `exports` map. No public-surface change.
- **PR #35553** `fix(core): unique telemetry user_id; expose workspace_id dimension` (2026-05-04) — internal analytics only.

### Carried from 2026-04 (still drifting at HEAD `0f5b8f4`)
- 🔴 `JsonInput` named-input variant (PR #35248, merged 2026-04-10, commit `77927e15f6`) — still **absent** from `astro-docs/src/content/docs/reference/inputs.mdoc` (covers `fileset` / `runtime` / `env` / `dependentTasksOutput` / `externalDependencies`, no `json` section) and from `reference/nx-json.mdoc`
- 🟠 `@nx/vite` plugin `compiler` option (PR #35429, merged 2026-04-24, commit `6c6d399eaa`) — still absent from `astro-docs/src/content/docs/technologies/build-tools/vite/introduction.mdoc`. Source still defines `compiler?: 'tsc' | 'tsgo' | 'vue-tsc'` at `packages/vite/src/plugins/plugin.ts:51`
- 🟠 `skipProjectGraph` flag (PR #35170, merged 2026-04-09, commit `b3483b3981`) — still read at `packages/nx/src/command-line/generate/generate.ts:315,433`, still not registered in `withGenerateOptions` yargs builder, still no `--help` exposure
- 🟠 4 `@deprecated` devkit APIs marked for removal in **Nx v21** — repo now on **23.0.0-beta.4** (`81e7d472fe`, 2026-05-02), **2 majors overdue**, no further escalation but no resolution
- 🟠 `devkit-internals` 30-export surface still imported by 11 first-party packages
- 🟡 `nx.sync.ignoredDependencies` (PR #35401, merged 2026-04-23) — still absent from `concepts/sync-generators.mdoc` and `reference/nx-json.mdoc`. Sibling `ignoredReferences` also still undocumented
- 🟡 7 ungrouped exports in `devkit-exports.ts` (lines 43, 45, 64, 69, 165, 249, 261) — no `@category` JSDoc; cosmetic for TypeDoc grouping. File length unchanged at 261 lines

## 🔴 High — Docs are wrong or missing

### packages/nx: `JsonInput` input type (`json`, `fields`, `excludeFields`)
- **Exported from**: `packages/nx/src/native/index.d.ts`, surfaced via `packages/nx/src/config/workspace-json-project-json.ts` as a member of the `inputs` union
- **Issue**: Public named-input variant absent from `reference/inputs.mdoc` (covers `fileset` / `runtime` / `env` / `dependentTasksOutput` / `externalDependencies`) and from `reference/nx-json.mdoc`. `fields` / `excludeFields` options absent from user-facing docs
- **User impact**: Users can't discover the JSON-field-scoped input type without reading `.d.ts` or source
- **Fix**: Add a section to `reference/inputs.mdoc` documenting `{ json, fields?, excludeFields? }` alongside the other named-input shapes
- **Origin**: PR #35248 (open since 2026-03 cycle, merged 2026-04-10 — drifting for ~25 days)

## 🟠 Medium — Stale or mismatched

### @nx/vite: `compiler` plugin option not in vite docs
- **Source**: `packages/vite/src/plugins/plugin.ts:46-51` — `compiler?: 'tsc' | 'tsgo' | 'vue-tsc'`
- **Issue**: New plugin option (PR #35429, merged 2026-04-24) absent from `astro-docs/src/content/docs/technologies/build-tools/vite/introduction.mdoc`. Auto-doc pipeline only covers generator/executor schemas, not inferred-plugin option types
- **User impact**: Users on TS 7 / tsgo cannot discover `compiler: 'tsgo'` without reading source or release notes
- **Fix**: Add a "Plugin options" block to vite intro listing `compiler`, mirroring `@nx/js` plugin docs

### packages/nx: `skipProjectGraph` flag undocumented and unregistered
- **Source**: `packages/nx/src/command-line/generate/generate.ts:315,433` — handler reads `args.skipProjectGraph` but `command-object.ts` `withGenerateOptions` never declares it
- **Issue**: Hidden flag works via untyped args; `--help` output won't show it; no docs page mentions it
- **Fix**: Either register it in yargs `withGenerateOptions` (preferred) or add a hidden-flag note in `reference/nx-commands` for `generate`
- **Origin**: PR #35170 (carried from 2026-04 → 2026-05; ~26 days drifting)

### @nx/devkit: deprecated APIs retained past stated removal version
- `addPluginV1` (`packages/devkit/src/utils/add-plugin.ts:58-62`) — "Use `addPlugin` instead"
- `getDependencyVersionFromPackageJson(Tree, …)` overload (`packages/devkit/src/utils/package-json.ts`)
- `js` / `jsOptionName` options on `ArtifactGenerationOptions` (`packages/devkit/src/generators/artifact-name-and-directory-utils.ts:30-37`) — both marked "will be removed in Nx v21"; repo now on **v23.0.0-beta.4**, so **2 majors overdue**
- **Note**: Previous report cited `name`/`suffix` at lines 31, 35 — that was a misread. The actual deprecated members are `js?: boolean` and `jsOptionName?: string` (`name`/`suffix` remain non-deprecated members of the same type at lines 25, 27). Severity unchanged
- **Fix**: Either remove (window has passed twice) or add a `reference/Deprecated/` callout

### devkit-internals usage across first-party plugins
- `packages/nx/src/devkit-internals.ts` (49 lines, ~30 exports) imported by 11 first-party packages (`@nx/next`, `@nx/cypress`, `@nx/angular`, `@nx/js`, `@nx/maven`, `@nx/module-federation`, `@nx/gradle`, `@nx/webpack`, `@nx/react-native`, `@nx/expo`, `@nx/rspack`)
- **Issue**: "Internal" surface is de facto public within the monorepo; any signature change has wide blast radius
- **Fix**: Stability-tier note in devkit reference or a contract test covering the set

## 🟡 Low — Missing JSDoc / minor

| Package | Symbol | Notes |
|---------|--------|-------|
| `@nx/devkit` | 7 ungrouped exports in `devkit-exports.ts` | Lines 43, 45, 64, 69, 165, 249, 261 — no `@category` JSDoc; cosmetic for TypeDoc grouping |
| `@nx/js` | `nx.sync.ignoredDependencies` | New config key (PR #35401, merged 2026-04-23); not in `concepts/sync-generators.mdoc` or `reference/nx-json.mdoc`. Sibling `ignoredReferences` also undocumented |
| `nx` | `WorkspaceFileMap.allWorkspaceFiles` (restored as `@deprecated` shim, PR #35518) | Properly JSDoc'd as deprecated in source. No user-doc page mentions it (it's an interface member of an internal type). Tracking only as informational signal of v22→v23 deprecation churn |

## ℹ️ Info — Internal API leaks

### devkit-internals consumers (informational)
30 symbols intended as semi-public within the monorepo, imported by 11 first-party packages. See Medium item.

### Ocean `nx-cloud` package public surface
`libs/nx-packages/nx-cloud/index.ts` exports only `nxCloudTasksRunnerShell` as default. The `bin/nx-cloud.ts` CLI delegates to a downloaded bundle (`light-client/update-manager`); subcommands are dynamic and not statically discoverable. Documented surface lives in `nx.app/docs`, which is out of scope for this audit. **Ocean clone failed this run** — verified via `npm view nx-cloud time --json` that no new stable releases shipped since 2026-03-31, so prior conclusions hold.

## Generators/Executors Without Doc Pages
None. All 33 packages with `generators.json` (and 28 with `executors.json`) are either mapped via `plugin-mappings.ts` `pluginToTechnology` (37 entries) or rendered by the dedicated `NxReferencePackagesLoader` (`nx`, `devkit`, `plugin`, `web`, `workspace`, CNW, CLI). New `feat(detox)!` `x-deprecated` schema annotations from PR #35529 picked up automatically.

## Leaked Internal APIs
See Info item "devkit-internals consumers" (30 symbols, 11 consumers). No new leaks vs 2026-04.

## Methodology Notes

- **Live data**: Scanned HEAD of `/tmp/nx-scan-1777915188` (shallow clone, HEAD `0f5b8f4`, dated 2026-05-04). Per-PR attribution for 2026-04-28 → 2026-05-04 cross-referenced against `~/projects/nx` git log since the scan clone is depth=1.
- **PR state verification**: `gh pr view` was authorization-timing-out this run; verified merge state via commit SHA presence in user-repo `git log` (all 4 carried-forward PRs — #35429, #35401, #35248, #35170 — confirmed merged with their reported SHAs, now `~25 days drifting`). No PRs were re-opened or reverted.
- **Ocean fallback**: Clone failed; used `npm view nx-cloud` to confirm no stable releases this window (last stable 19.1.3, 2026-03-31). Prior `libs/nx-packages/nx-cloud/index.ts` shape stands.
- **Auto-doc coverage check**: Re-confirmed all generator-bearing packages mapped. The 4 packages flagged as "unmapped" by a naive grep (`nx`, `react-native`, `web`, `workspace`) all resolve correctly: `react-native` is mapped under single quotes; `nx` / `web` / `workspace` are routed through `NxReferencePackagesLoader` rather than `pluginToTechnology`.
- **Confidence**: High for HEAD state. Lookback windowing relies on user-repo git log (56 commits in window). PR-state verification depended on commit SHA presence rather than `gh pr view` due to auth timeout — note as a one-cycle methodological softness.

