# API Surface Audit — 2026-04

_Last updated: 2026-04-24_
_Scope: Nx CLI packages, devkit public API, astro-docs auto-generated reference_
_Nx repo: `/tmp/nx-scan-87586` (shallow clone, HEAD `081cb11`)_
_Ocean repo: **Not audited this cycle** (clone failed); ocean/public-sdk drift carries over from prior assessment_

## Summary

| Severity | Count | Δ vs 2026-03 |
|----------|------:|-------------:|
| 🔴 High  | 1     | −1 |
| 🟠 Medium | 3     | 0 |
| 🟡 Low   | 2     | 0 |
| **Total** | **6** | **−1** |

- Public exports checked: `devkit-exports.ts` (261 lines), `devkit-internals.ts` (49 lines)
- Nx packages with generators/executors: 38 — **100% mapped** in astro-docs `plugin-mappings.ts`
- Unmapped plugin packages with gens/execs: **0** (down from 10 in 2026-03)

## Changes Since Last Month

### Resolved / no longer applicable (architectural, not content fixes)
- **All 10 "undocumented packages"** from 2026-03 are auto-documented via the astro-docs plugin loader (`astro-docs/src/plugins/plugin.loader.ts` + `plugin-mappings.ts`). Tech dirs exist under `astro-docs/src/content/docs/technologies/` for docker, gradle, maven, playwright, remix, rspack, vitest, nuxt, rsbuild, eslint.
- **"Stale docs" (tao, linter, angular-rsbuild)** from 2026-03 no longer apply — legacy `nx-dev/packages.json` was replaced by the astro-docs auto-scan. No stale package listings remain.
- **Per-package generator/executor drift across 20+ packages** from 2026-03 is now auto-sourced from `generators.json` / `executors.json` at build time (`astro-docs/src/plugins/utils/plugin-schema-parser.ts`). Those findings were largely extraction artifacts from the old pipeline; the new pipeline makes the class structurally impossible.
- **Deprecated CLI commands** (`print-affected`, `affected:graph`) have dedicated pages under `reference/Deprecated/`.

### Still drifting (carried from 2026-03)
- 7 missing `@category` JSDoc blocks in `devkit-exports.ts` (lines 43, 45, 64, 69, 165, 249, 261) — **unchanged**.
- 4 `@deprecated` devkit APIs retained past their stated removal version; v21-marked items still shipping in v22.
- `JsonInput` input type (PR #35248) — still undocumented in `reference/inputs.mdoc` / `reference/nx-json.mdoc`.

### New drift since last run
- **None detected at HEAD.** Lookback `git log` is constrained by shallow clone (depth=1, single commit `081cb11`), so per-PR attribution inside 2026-02-23 → 2026-04-24 cannot be fully reconstructed locally.

### Resolved since last run
- **Undocumented packages bucket** — collapsed structurally by astro-docs loader coverage reaching 100%. Net delta: −1 high severity.

## 🔴 High — Docs are wrong or missing

### packages/nx: `JsonInput` input type (`json`, `fields`, `excludeFields`)
- **Exported from**: `packages/nx/src/native/index.d.ts:462`, re-exposed via `packages/nx/src/config/workspace-json-project-json.ts:222` as a member of the `inputs` union
- **Issue**: Public named-input variant not referenced in `astro-docs/src/content/docs/reference/inputs.mdoc` or `nx-json.mdoc`. `fields` / `excludeFields` options absent from user-facing docs.
- **User impact**: Users can't discover the JSON-field-scoped input type without reading `.d.ts` or source
- **Fix**: Add a section to `reference/inputs.mdoc` documenting `{ json, fields?, excludeFields? }` alongside `fileset`, `runtime`, etc.
- **Origin**: PR #35248 (flagged in 2026-03, still open)

## 🟠 Medium — Stale or mismatched

### @nx/devkit: 7 exports missing `@category` JSDoc
- **File**: `packages/nx/src/devkit-exports.ts`
- **Lines**: 43 (`workspaceLayout`), 45 (`NxPlugin*` types), 64 (`AggregateCreateNodesError`, `StaleProjectGraphCacheError`), 69 (`createNodesFromFiles`), 165 (`GraphJson`), 249 (`hashArray`), 261 (`isDaemonEnabled`)
- **Issue**: TypeDoc groups ungrouped symbols into a default bucket, hurting reference-doc navigation
- **Fix**: Add `/** @category … */` JSDoc blocks; trivial, consistent with surrounding exports

### @nx/devkit: deprecated APIs retained past stated removal version
- `addPluginV1` (`devkit/src/utils/add-plugin.ts:58`) — "Use `addPlugin` instead"
- `getDependencyVersionFromPackageJson(Tree, …)` overload (`devkit/src/utils/package-json.ts:832`)
- `name` / `suffix` options in artifact utils (`devkit/src/generators/artifact-name-and-directory-utils.ts:31,35`) — marked for removal in **Nx v21**; current Nx is past v21, so these are overdue for removal
- **Fix**: Either remove (since the removal window has passed) or add a deprecation callout page under `reference/Deprecated/`

### devkit-internals usage across first-party plugins
- `devkit-internals.ts` (49 lines, 30 exports) imported by 11 first-party packages (`@nx/next`, `@nx/cypress`, `@nx/angular`, `@nx/js`, `@nx/maven`, `@nx/module-federation`, `@nx/gradle`, `@nx/webpack`, `@nx/react-native`, `@nx/expo`, `@nx/rspack`)
- **Issue**: "Internal" surface is de facto public within the monorepo; any signature change has wide blast radius
- **Fix**: Stability-tier note in devkit reference or a contract test covering the set

## 🟡 Low — Missing JSDoc / minor

| Package | Symbol | Notes |
|---------|--------|-------|
| `@nx/devkit` | 7 exports (see Medium above) | Purely cosmetic for TypeDoc grouping |
| `create-nx-workspace` | `@deprecated Use CnwError instead` on legacy error util (`src/utils/error-utils.ts:43`) | Internal, no docs impact; flag only if leaked |

## Generators/Executors Without Doc Pages
None. All 38 plugin packages with generators/executors are mapped in `plugin-mappings.ts` and auto-rendered via `plugin.loader.ts`.

## Leaked Internal APIs
See Medium item "devkit-internals usage across first-party plugins" (30 symbols, 11 consumers). No new leaks vs 2026-03.

## Methodology Notes

- **Live data**: Scanned HEAD of `/tmp/nx-scan-87586` (shallow clone, 1 commit). Only the merge commit `081cb11` (docs link fix) is visible inside the lookback window. Per-PR attribution for 2026-02-23 → 2026-04-24 relies on the prior report plus current code state.
- **Astro-docs pivot**: This cycle confirmed that the 2026-03 report's "undocumented packages / stale docs / per-package generator drift" section was measured against the legacy `nx-dev/packages.json` registry, which is gone. The current pipeline auto-derives reference docs from `packages/*/generators.json|executors.json|migrations.json` plus `plugin-mappings.ts`, so those drift classes are now structurally impossible. Methodology narrowed to:
  1. Public devkit exports (manual curation + JSDoc quality)
  2. Auto-doc coverage gaps in `plugin-mappings.ts` (currently 0)
  3. Hand-written reference docs that must track code (`inputs.mdoc`, `nx-json.mdoc`, non-auto-generated CLI pages)
  4. Deprecated API surfacing and lifecycle
- **Ocean not audited this cycle**: clone failed; prior findings carry over unchanged.
- **No net new high-severity drift introduced this window**. The sole live drift (`JsonInput`, PR #35248) was already flagged last month.
