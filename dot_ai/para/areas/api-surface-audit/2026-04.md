# API Surface Audit — 2026-04

_Last updated: 2026-04-28_
_Scope: Nx CLI packages, devkit public API, astro-docs auto-generated reference, ocean publishable packages_
_Nx repo: `/tmp/nx-scan-1777382403` (shallow clone, HEAD `0e779e5`)_
_Ocean repo: `/tmp/ocean-scan-real` (shallow clone, HEAD `a619837`)_

## Summary

| Severity | Count | Δ vs 2026-03 | Δ vs 2026-04-22 |
|----------|------:|-------------:|----------------:|
| 🔴 High  | 1     | −1           | 0               |
| 🟠 Medium | 4     | +1           | +1              |
| 🟡 Low   | 3     | +1           | +1              |
| ℹ️ Info  | 2     | 0            | 0               |
| **Total actionable** | **8** | **+1** | **+2** |

- Public exports checked: `devkit-exports.ts` (261 lines), `devkit-internals.ts` (49 lines)
- Nx packages with generators/executors: 38 — **100% mapped** in `plugin-mappings.ts`
- Ocean publishable packages scanned: 12 (`nx-cloud`, `gcs-cache`, `s3-cache`, `azure-cache`, `shared-fs-cache`, `nx-key`, `conformance`, `owners`, `enterprise-cloud`, `nx-graph`, `diagnostics`, `powerpack-utils`)
- Ocean `nx-cloud` shell binary: thin re-exporter over downloaded bundle; no public TS surface to drift

## Changes Since Last Run (2026-04-22 → 2026-04-28)

### New drift introduced this window
- 🟠 **`@nx/vite` plugin `compiler` option** (PR #35429, commit `6c6d399eaa`, 2026-04-24) — adds `compiler?: 'tsc' | 'tsgo' | 'vue-tsc'` to vite plugin options for tsgo support. JSDoc on the option exists in source, but `astro-docs/src/content/docs/technologies/build-tools/vite/introduction.mdoc` does not mention `compiler` or `tsgo`. Auto-doc pipeline only covers generators/executors, not inferred-plugin option schemas.
- 🟡 **`nx.sync.ignoredDependencies`** (PR #35401, commit `82e7afe455`, 2026-04-23) — new `nx.sync` config key under `@nx/js:typescript-sync`. Not referenced in `astro-docs/src/content/docs/concepts/sync-generators.mdoc` or `reference/nx-json.mdoc`. The sibling `nx.sync.ignoredReferences` was never documented either.

### Resolved this window
- None. The 2026-04-22 baseline items (JsonInput, skipProjectGraph, devkit JSDoc, deprecated-past-removal-version) all still present at HEAD `0e779e5`.

### Carried from 2026-03 (still drifting)
- `JsonInput` named-input variant (PR #35248) — still undocumented in `reference/inputs.mdoc` and `reference/nx-json.mdoc`
- 7 missing `@category` JSDoc blocks in `devkit-exports.ts` (lines 43, 45, 64, 69, 165, 249, 261)
- 4 `@deprecated` devkit APIs marked for removal in Nx v21 — repo is now on **23.0.0-beta.0** (commit `0a679ad822`, 2026-04-25), so these are 2 majors overdue
- `skipProjectGraph` flag (PR #35170) — referenced by `generate.ts:315` and `:433` but not registered in `command-object.ts` yargs builder; not in user-facing docs

### Resolved structurally last cycle (no new regressions)
- All 10 "undocumented packages" from 2026-03 remain mapped via `astro-docs/src/plugins/utils/plugin-mappings.ts` (53 entries) and rendered by `plugin.loader.ts`
- New `dotnet:ci-workflow` generator (PR #33321, 2026-04-25) — auto-doc'd via `generators.json` → `plugin-schema-parser.ts` pipeline, no manual page needed
- Spread-token target merge (PR #34285, 2026-04-17) — already documented in `reference/project-configuration.mdoc:636-688`

## 🔴 High — Docs are wrong or missing

### packages/nx: `JsonInput` input type (`json`, `fields`, `excludeFields`)
- **Exported from**: `packages/nx/src/native/index.d.ts:462`, surfaced via `packages/nx/src/config/workspace-json-project-json.ts:222` as a member of the `inputs` union
- **Issue**: Public named-input variant not referenced in `reference/inputs.mdoc` or `reference/nx-json.mdoc`. `fields` / `excludeFields` options absent from user-facing docs.
- **User impact**: Users can't discover the JSON-field-scoped input type without reading `.d.ts` or source
- **Fix**: Add a section to `reference/inputs.mdoc` documenting `{ json, fields?, excludeFields? }` alongside `fileset`, `runtime`, etc.
- **Origin**: PR #35248 (open since 2026-03 cycle)

## 🟠 Medium — Stale or mismatched

### @nx/vite: new `compiler` plugin option not in vite docs
- **Source**: `packages/vite/src/plugins/plugin.ts:45-50` — `compiler?: 'tsc' | 'tsgo' | 'vue-tsc'`
- **Issue**: New plugin option (PR #35429) absent from `astro-docs/src/content/docs/technologies/build-tools/vite/introduction.mdoc`. Auto-doc pipeline only covers generator/executor schemas, not inferred-plugin option types.
- **User impact**: Users on TS 7 / tsgo cannot discover `compiler: 'tsgo'` without reading source or release notes
- **Fix**: Add a "Plugin options" block to vite intro listing `compiler`, mirroring `@nx/js` plugin docs

### packages/nx: `skipProjectGraph` flag undocumented and unregistered
- **Source**: `packages/nx/src/command-line/generate/generate.ts:315,433` — handler reads `args.skipProjectGraph` but `command-object.ts` `withGenerateOptions` never declares it
- **Issue**: Hidden flag works via untyped args; `--help` output won't show it; no docs page mentions it
- **Fix**: Either register it in yargs `withGenerateOptions` (preferred) or add a hidden-flag note in `reference/nx-commands` for `generate`
- **Origin**: PR #35170 (carried)

### @nx/devkit: deprecated APIs retained past stated removal version
- `addPluginV1` (`packages/devkit/src/utils/add-plugin.ts:58`) — "Use `addPlugin` instead"
- `getDependencyVersionFromPackageJson(Tree, …)` overload (`packages/devkit/src/utils/package-json.ts:832`)
- `name` / `suffix` options in artifact utils (`packages/devkit/src/generators/artifact-name-and-directory-utils.ts:31,35`) — marked for removal in **Nx v21**; repo now on **v23.0.0-beta.0**, so these are **2 majors overdue**
- **Fix**: Either remove (window has passed twice) or add a `reference/Deprecated/` callout. Severity bumped from "overdue" to "2-majors overdue" since 2026-03.

### devkit-internals usage across first-party plugins
- `packages/nx/src/devkit-internals.ts` (49 lines, ~30 exports) imported by 11 first-party packages (`@nx/next`, `@nx/cypress`, `@nx/angular`, `@nx/js`, `@nx/maven`, `@nx/module-federation`, `@nx/gradle`, `@nx/webpack`, `@nx/react-native`, `@nx/expo`, `@nx/rspack`)
- **Issue**: "Internal" surface is de facto public within the monorepo; any signature change has wide blast radius
- **Fix**: Stability-tier note in devkit reference or a contract test covering the set

## 🟡 Low — Missing JSDoc / minor

| Package | Symbol | Notes |
|---------|--------|-------|
| `@nx/devkit` | 7 ungrouped exports in `devkit-exports.ts` | Lines 43, 45, 64, 69, 165, 249, 261 — no `@category` JSDoc; cosmetic for TypeDoc grouping |
| `@nx/js` | `nx.sync.ignoredDependencies` | New config key (PR #35401); not in `concepts/sync-generators.mdoc` or `reference/nx-json.mdoc`. Sibling `ignoredReferences` also undocumented. |
| `create-nx-workspace` | `@deprecated Use CnwError instead` on legacy error util (`src/utils/error-utils.ts:43`) | Internal, no docs impact; flag only if leaked |

## ℹ️ Info — Internal API leaks

### devkit-internals consumers (informational)
30 symbols intended as semi-public within the monorepo, imported by 11 first-party packages. See Medium item.

### Ocean `nx-cloud` package public surface
`libs/nx-packages/nx-cloud/index.ts` exports only `nxCloudTasksRunnerShell` as default. The `bin/nx-cloud.ts` CLI delegates to a downloaded bundle (`light-client/update-manager`); subcommands are dynamic and not statically discoverable. Documented surface lives in `nx.app/docs`, which is out of scope for this audit. No drift detectable from source; flagged as info only.

## Generators/Executors Without Doc Pages
None. All 38 plugin packages with generators/executors are mapped in `plugin-mappings.ts` (53 entries) and auto-rendered via `plugin.loader.ts` + `plugin-schema-parser.ts`. New `dotnet:ci-workflow` generator from this window picks up automatically.

## Leaked Internal APIs
See Info item "devkit-internals consumers" (30 symbols, 11 consumers). No new leaks vs 2026-03.

## Methodology Notes

- **Live data**: Scanned HEAD of `/tmp/nx-scan-1777382403` (shallow clone, HEAD `0e779e5`) and `/tmp/ocean-scan-real` (HEAD `a619837`). Per-PR attribution for 2026-04-22 → 2026-04-28 cross-referenced against user-repo `git log` since the scan clones are depth=1.
- **Astro-docs pivot (recap)**: Reference docs for plugin generators/executors are auto-derived from `packages/*/generators.json|executors.json|migrations.json` plus `plugin-mappings.ts`, so the 2026-03 "undocumented packages / per-package generator drift" classes are structurally impossible. Methodology now narrows to:
  1. Public devkit exports (manual curation + JSDoc quality)
  2. Auto-doc coverage gaps in `plugin-mappings.ts` (currently 0)
  3. Hand-written reference docs that must track code (`inputs.mdoc`, `nx-json.mdoc`, `nx-commands.mdoc`, `concepts/sync-generators.mdoc`, non-auto-generated CLI pages)
  4. **Inferred-plugin option schemas** — auto-doc pipeline does NOT cover these; new options on `@nx/vite`, `@nx/js`, `@nx/playwright`, etc. plugins must be hand-documented in each tech intro page
  5. Deprecated API surfacing and lifecycle (now compounding — v21-marked APIs still ship in v23.0.0-beta.0)
- **Ocean scope**: Only `libs/nx-packages/*` packages with `publishConfig` were scanned. Most are caching adapters with thin public APIs gated by `@nx/key`. `nx-cloud` shell delegates to downloaded bundle; runtime CLI commands not statically discoverable.
- **Confidence**: High for HEAD state. Lookback windowing relies on user-repo git log since the scan clones are shallow.
