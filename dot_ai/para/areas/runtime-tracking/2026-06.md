# Runtime Tracking -- June 2026

_Last updated: 2026-06-15_

**Lookback delta (W24 update)**: 2026-06-01 -> 2026-06-15 (14-day delta since June-01 scan)

**Lookback delta (June-01 scan)**: 2026-05-25 -> 2026-06-01 (7-day delta since W22 scan; June scan is new)

---

## ⚡ Action Items

_Updated 2026-06-15 (W24 scan)_

| Priority | Item | Notes |
|----------|------|-------|
| URGENT | **Node 20 is 46 days post-EOL, `engines` field still null on nx@22.7.5** | **8th consecutive flag.** Users on Node 20 have now gone 6+ weeks without upstream security patches. Ship `"engines": { "node": ">=20.0.0" }` + runtime deprecation warning immediately. No signs of progress. |
| URGENT | **Node 25 CI matrix cleanup (due 2026-06-01) -- confirm done** | Node 25 EOL was June 1. If Nx CI matrix still tests Node 25, it is testing an unsupported runtime. Verify and remove. |
| HIGH | **Node 26.3.0: `--permission` flag graduated from experimental (2026-06-01)** | If any Nx script or CI template uses `--experimental-permission`, the flag name changed. Audit Nx codebase and docs for `--experimental-permission` references and update to `--permission`. |
| HIGH | **TC39: Decorators + Decorator Metadata confirmed at Stage 2.7 (regression)** | Still at Stage 2.7 as of 2026-06-15 fetch -- no reversal. Angular integrations affected. Watch TypeScript team response. |
| HIGH | **TC39: `Explicit Resource Management` (`using`) confirmed Stage 4** | Absent from Stage 3 list in two consecutive fetches. Now official ECMAScript. TypeScript `using`/`await using` is spec-compliant. Audit Nx for any `using` polyfill or workaround. |
| MEDIUM | **Node.js security release incoming ~2026-06-17** | Blog listed a "June 17 Security Releases" post (2 days after this scan). Watch for CVEs affecting Node 22/24/26 and update Nx CI base images accordingly. |
| MEDIUM | **Deno 2.8.2 + 2.8.3 (June 3 + June 11): rapid iteration** | Post-quantum crypto (ML-DSA/ML-KEM), native http2 OTel, task runner env-file flag. Deno workspace story continues to mature. See Deno section for details. |
| MEDIUM | **Node 26.3.0 `permission.drop()` API** | New runtime API to drop capabilities at runtime. If Nx sandboxing/task isolation ever uses Node permissions model, this is now stable. |
| MEDIUM | Bun lockfile #16252 still dead (7+ months, 0 assignees, no PR) | No new Bun releases since 1.3.14 (May 13). 33 days without a Bun release -- unusually quiet. Decide if Nx ships a workaround. |
| LOW | **Node 26 macOS x64 downgraded to Tier 2** | Apple deprecating Intel. Node 26 universal binaries (x64 + arm64) will continue "as long as practical" but not guaranteed for full v26 lifetime. No immediate Nx action, but note for CI runner provisioning. |

---

## W24 Update (2026-06-15)

### Node.js

#### New releases since 2026-06-01

| Version | Date | Type | Key Changes |
|---------|------|------|-------------|
| v26.3.0 | 2026-06-01 | Minor Current | `--permission` graduates (no longer experimental), `permission.drop()` API, Buffer.poolSize -> 64 KiB, npm 11.16.0, QUIC rate limits + ECN + batching, WebCrypto prototype pollution hardening, macOS x64 Tier 2, root certs NSS 3.123.1 |

No new Node 24 or 22 releases since May.

**Heads-up**: nodejs.org blog lists a "June 17 Security Releases" entry (2 days after this scan). Likely affects v22/v24/v26. Watch for patch versions and update CI base images.

#### LTS lifecycle (re-verified 2026-06-15 via nodejs.org/en/about/previous-releases)

| Line | Codename | Status | Latest | EOL |
|------|----------|--------|--------|-----|
| v26 | -- | **Current** | 26.3.0 | Oct 2026 |
| v25 | -- | **EOL** | 25.9.0 | 2026-06-01 |
| v24 | Krypton | **Active LTS** | 24.16.0 | 2028-04-30 |
| v22 | Jod | **Maintenance LTS** | 22.22.3 | 2027-04-30 |
| v20 | Iron | **EOL (46 days)** | 20.20.2 | 2026-04-30 |

#### New Risks for Nx (v26.3.0)

- **`--experimental-permission` -> `--permission`**: Flag is now stable. Any Nx-generated scripts or docs referencing `--experimental-permission` are now incorrect. Grep needed.
- **`permission.drop()`**: New stable API to drop runtime permissions (e.g., fs, net). If Nx task sandboxing evolves to use Node permissions model, this is now a stable foundation.
- **Buffer.poolSize 64 KiB**: Memory allocation pattern change. Affects buffer-heavy code paths. Unlikely to break Nx but worth monitoring in Node 26 CI runs.
- **macOS Intel Tier 2**: Node 26 may not provide universal binaries for full v26 lifetime. CI runners provisioned on Intel Mac hardware should plan migration.

#### New Opportunities (v26.3.0)

- **Inspector coverage API exposed**: More reliable code coverage in Node 26 test runs. Beneficial for Nx's test runner integration.
- **npm 11.16.0 bundled**: Updated npm in Node 26 environment. If Nx Cloud runners or local Nx installs use the bundled npm, version drift between Node versions widens.

---

### TC39 (W24 fetch -- 2026-06-15)

No stage changes since the June-01 scan. Both Stage 3 and Stage 2.7 landscapes are identical to the June-01 fetch.

- **Confirmed Stage 4**: Explicit Resource Management (`using`), Atomics.pause, Joint Iteration -- absent from Stage 3 list in two consecutive fetches (June-01 and June-15).
- **Confirmed Stage 2.7**: Decorators, Decorator Metadata -- regression holds. No reversal.
- **No new Stage 3 advancements** in the June 1-15 window. Next TC39 plenary expected July 2026.

---

### Bun (W24)

No new releases. v1.3.14 (May 13) is still latest as of June 15. 33-day release gap -- unusually quiet for Bun. No movement on lockfile #16252.

---

### Deno (W24)

| Version | Date | Key Changes |
|---------|------|-------------|
| v2.8.2 | 2026-06-03 | Post-quantum crypto (ChaCha20-Poly1305, SHAKE, ML-DSA, ML-KEM), bundle compile mode + minify, Jupyter kernel rewritten in JS (zeromq dropped), task runner env-file flag + wildcard exclusion groups, node:http env/global proxy, native http parser improvements |
| v2.8.3 | 2026-06-11 | Compile watch mode, glob in `links` config, auto-discover external import maps, SubtleCrypto.supports(), ML-DSA JWK import/export, **native http2 OTel auto-instrumentation**, improved socket lifecycle, Fetch priority field, Deno.readDir streaming |

#### Highlights for Nx

- **Native http2 OTel auto-instrumentation (v2.8.3)**: Deno can now instrument outgoing http2 connections without code changes. Relevant for Nx Cloud connectivity diagnostics in Deno environments. No Nx action needed, but improves observability for Deno-first Nx Cloud users.
- **Task runner env-file flag (v2.8.2)**: Deno tasks now support `--env-file`, matching a common Nx workflow pattern. Reduces friction for Deno-on-Nx setups.
- **Wildcard exclusion groups in task runner (v2.8.2)**: `deno task foo:*` but `!foo:skip` syntax. Approaching Nx target pattern filtering.
- **Post-quantum crypto (v2.8.2)**: ML-DSA + ML-KEM in Deno, same as Node 26.2.0. Both runtimes now have PQC. Deno is matching Node's security feature pace.
- **SubtleCrypto.supports() (v2.8.3)**: New API to probe algorithm support at runtime. Node does not yet have this.

**Risk level for Nx: LOW-MEDIUM (unchanged)**. Two rapid releases but none introduce Nx compat regressions. Deno workspace and task runner continue to narrow the gap with Nx patterns.

---

## Node.js (June-01 scan)

### Releases since 2026-05-25

No new Node.js releases since the W22 scan (last: 24.16.0 on May 21, 26.2.0 on May 20). June starts with no new releases.

| Version | Date | Type | Key Changes |
|---------|------|------|-------------|
| v24.16.0 | 2026-05-21 | Minor LTS (Krypton) | `randomUUIDv7()`, `fs.stat` signal option, test runner randomization, `req.signal` on `IncomingMessage`, hex colors in `util.styleText` |
| v26.2.0 | 2026-05-20 | Minor Current | `stream.compose` -> stable, `Temporal.Instant` on `Stats`/`BigIntStats`, `writeInformation()` for 1xx HTTP, ML-DSA/ML-KEM crypto (BoringSSL), QUIC internal impl complete, undici 8.3.0 |
| v22.22.3 | 2026-05-13 | Patch LTS (Jod) | Security/bug fixes |

### LTS Lifecycle (confirmed 2026-06-01)

| Line | Codename | Status | EOL |
|------|----------|--------|-----|
| v26 | -- | **Current** | ~2026-11 |
| v25 | -- | **EOL (TODAY 2026-06-01)** | 2026-06-01 |
| v24 | Krypton | **Active LTS** | 2028-04-30 |
| v22 | Jod | **Maintenance LTS** | 2027-04-30 |
| v20 | Iron | **EOL (32 days)** | 2026-04-30 |

### Risks for Nx

- **Node 25 EOL today**: Any CI matrix pinned to Node 25 is now testing against an unsupported runtime. Drop it in favor of Node 24 LTS + Node 26 Current.
- **Node 26 `module.register()` runtime-deprecated (DEP0205)**: Plugin loaders using `module.register()` will emit runtime warnings on Node 26. Confirm Nx plugin hook chains don't trigger this.
- **Node 26 `_stream_*` legacy modules removed**: Grep Nx packages and dependencies for `require('_stream_readable')` etc. Likely already clean but must verify.
- **Node 26 `Temporal` integration**: `Stats.mtime` can now return a `Temporal.Instant` in Node 26+. Code that calls `.getTime()` or passes `mtime` to `Date` constructor may behave differently.
- **Node 20 users in production**: Now 32 days without upstream CVE patches. Nx should surface a deprecation warning.

### Opportunities

- **Node 26 `stream.compose` stable**: Replace any manually composed pipeline helpers.
- **Node 24.16 `randomUUIDv7()`**: Monotonic, time-sortable UUIDs. Relevant for Nx Cloud task IDs, cache keys where ordering matters.
- **`require(esm)` stable in Node 24.15+ and Node 26**: ESM plugin loading via `require()` is now standard. Simplifies Nx plugin host code.
- **Module compile cache stable (Node 24.15+)**: `node --module-cache` via API. Benchmark potential Nx CLI startup savings.

---

## TC39 Proposals

### Stage changes since May 2026 scan

_Source: tc39/proposals README, fetched 2026-06-01. May TC39 plenary results now reflected._

| Proposal | Prior Stage | Current Stage | Nx Impact |
|----------|-------------|---------------|-----------|
| Explicit Resource Management (`using`) | Stage 3 | **Stage 4 (likely)** | Now official ECMAScript. TypeScript already ships `using`/`await using`. Polyfill audit. |
| Atomics.pause | Stage 3 | **Stage 4 (likely)** | Worker thread performance -- low direct Nx impact |
| Joint Iteration | Stage 3 | **Stage 4 (likely)** | Iterating parallel iterables -- low direct Nx impact |
| Iterator Chunking | Stage 2.7 | **Stage 3** | Batching generator output -- affects Nx task streaming |
| Iterator Includes | Stage 2.7 | **Stage 3** | Minor -- replaces manual `.find()` patterns |
| Iterator Join | Stage 2.7 | **Stage 3** | Minor -- string join over iterables |
| Error Stack Accessor | Stage 2.7 | **Stage 3** | Standardized error stack access -- relevant to Nx diagnostics |
| RegExp Buffer Boundaries | new | **Stage 3** | Word boundary fix -- low impact |
| **Decorators** | Stage 3 | **Stage 2.7 (REGRESSION)** | Angular + NestJS generators depend on Decorator support assumptions. Investigate if this affects TypeScript's Decorator impl. |
| **Decorator Metadata** | Stage 3 | **Stage 2.7 (REGRESSION)** | Same concern as Decorators. |

**Note on "Stage 4 (likely)"**: These three proposals no longer appear in the Stage 3 list in tc39/proposals README. Standard interpretation: graduated to Stage 4 (shipped in ES specification). Could not independently verify plenary minutes -- treat as high-confidence but confirm when TC39 May 2026 notes are published.

**Note on Decorators demotion**: Stage 3 -> Stage 2.7 regression is highly unusual. Stage 2.7 was introduced to mean "all spec text is final, no open issues" -- moving back suggests a reopened design concern. If confirmed, TypeScript's Decorator implementation could be at risk of diverging from the final spec. Watch for TypeScript team response.

### Stage 3 landscape (June 2026)

Legacy RegExp features, Source Phase Imports, Dynamic Code Brand Checks, Deferring Module Evaluation (Import Defer), Non-extensible Applies to Private, Import Text, Iterator Chunking, Iterator Includes, Iterator Join, Error Stack Accessor, RegExp Buffer Boundaries.

### Stage 2.7 landscape

**Decorators** (REGRESSION from Stage 3), **Decorator Metadata** (REGRESSION from Stage 3), ShadowRealm, ESM Phase Imports, Immutable ArrayBuffers, Import Bytes, Await Dictionary.

### Key carryovers

- **Import Text (Stage 3)**: Still actionable for Nx generator template loading once V8 ships.
- **Import Defer (Stage 3)**: Deno 2.8.0 ships unstable support. First runtime to ship it. Reduces pressure on Nx to polyfill lazy-loaded plugins.
- **Type Annotations (Stage 1)**: No movement. Node `--strip-types` remains the pragmatic path.

---

## Bun

### Releases since 2026-05-25

None. Last release: v1.3.14 (2026-05-13).

| Version | Date | Key Changes |
|---------|------|-------------|
| v1.3.14 | 2026-05-13 | 11 contributors, minor fixes, no orchestration or compat delta |

### Nx compat status

- **Lockfile #16252**: Still open, 0 comments since 2025-11-25, 0 assignees, no PR. 6+ months dead. Bun -> Yarn lockfile conversion is broken for workspace deps. This is the primary Nx-on-Bun blocker.
- **Test orchestration** (`--parallel/--isolate/--shard/--changed` from 1.3.13): Competitive overlap with Nx affected/shard. No new movement but still worth tracking adoption.
- **Node compat**: No regressions in 1.3.14. Stability.

**Risk level for Nx: MEDIUM-HIGH (unchanged)**. No new Bun releases to assess; the orchestration competitive vector introduced in 1.3.13 remains open.

---

## Deno

### Releases since 2026-05-25

| Version | Date | Key Changes |
|---------|------|-------------|
| **v2.8.1** | 2026-05-27 | TLS cert fixes, `node:util` additions (`getSystemErrorMap`, `transferableAbortSignal`), `fs.exists` fix, `fs.watch` error events, browser field map in `package.json` bundles, `sideEffects` field reading, BYONM `node_modules/.bin` walk, reverted compile cache companion |

### Highlights (v2.8.1)

- **`package.json` browser field map**: Deno's bundler now reads the `browser` field for package resolution. Affects how Nx-built libraries consumed by Deno tooling resolve browser vs node exports. Could expose dual-package issues in Nx libs.
- **BYONM `node_modules/.bin` walk**: Better binary resolution in bring-your-own-node-modules mode. Nx workspaces using Deno in BYONM mode should see improved CLI tool resolution.
- **Reverted compile cache companion (`module.enableCompileCache`)**: The Node.js polyfill for this was reverted in 2.8.1, suggesting incompatibilities. Module compile cache is not reliably available in Deno.
- **Security: resolved IPs against net deny list for fetch/WebSocket**: Relevant for Nx Cloud connectivity in sandboxed Deno environments.

### Nx compat status

- **`catalog:` protocol** (2.8.0): Deno workspace dep centralization now mirrors pnpm catalogs. Competitive signal -- Nx workspace dep management has a new peer in Deno.
- **Hoisted node_modules** (2.8.0): Deno workspaces can now use hoisted linking. Makes Deno more viable for monorepos.
- **Node compat**: Continued improvement. `node:http`, TLS, `node:util`, `node:repl` all tightened in 2.8.x.
- **`deno pack`/`deno ci`/`deno bump-version`** (2.8.0): Deno is building out a full dev-workflow CLI. Still less capable than Nx for large monorepos but gap is narrowing.

**Risk level for Nx: LOW -> LOW-MEDIUM** (nudge up: Deno workspace + catalog protocol + hoisted linker = a legitimately viable Nx alternative for small-to-mid Deno-first monorepos as of 2.8.x).

---

## Nx `engines` Field Review

**State re-verified 2026-06-01**:

```
npm view nx version  -> 22.7.5
npm view nx engines  -> (empty)
```

`engines` field is missing in published `nx@22.7.5`. **7th consecutive scan without this field.**

### Impact summary (June 2026)

| Node Version | Status as of 2026-06-01 | Recommended Nx Action |
|-------------|-------------------------|-----------------------|
| Node 18 | EOL (Apr 2025) | Dropped |
| Node 20 | **EOL (32 days)** | Emit deprecation warning in Nx 22.x now; drop in `engines` in Nx 23 |
| Node 22 | Maintenance LTS | Supported |
| Node 24 | **Active LTS** | Recommended, should be the primary CI target |
| Node 25 | **EOL as of TODAY** | Remove from test matrix immediately |
| Node 26 | Current | Supported with caveats (see Risks above) |

**Suggested immediate action** -- add to `packages/nx/package.json`:
```json
"engines": { "node": ">=20.0.0" }
```
Then in Nx 22 patch, add a runtime deprecation warning for Node 20 (mirrors how Node itself warns for EOL lines). Bump to `>=22.0.0` in Nx 23.

---

## Delta vs 2026-05 Report (W22 scan, 2026-05-25)

| Item | 2026-05-25 | 2026-06-01 | Change |
|------|------------|------------|--------|
| Node 25 EOL | "7 days" | **EOL TODAY** | On schedule |
| Node 20 post-EOL | 25 days | **32 days** | +7d. Still no `engines` field. |
| Latest Node Current | 26.2.0 | 26.2.0 | No change |
| Latest Node LTS | 24.16.0 | 24.16.0 | No change |
| Latest Bun | 1.3.14 | 1.3.14 | No change |
| Latest Deno | 2.8.0 | **2.8.1** | Patch: TLS, node:util, browser field map |
| Nx `engines` field | null (6th) | null | **7th consecutive miss** |
| Bun lockfile #16252 | dead (6mo) | dead (6mo+) | No change |
| TC39 Decorators | Stage 3 | **Stage 2.7 (REGRESSION)** | May plenary result |
| TC39 `using` / Explicit Resource Management | Stage 3 | **Stage 4 (likely)** | May plenary - graduated |
| TC39 Iterator Chunking/Includes/Join | Stage 2.7 | **Stage 3** | May plenary advancements |
| TC39 Error Stack Accessor | Stage 2.7 | **Stage 3** | May plenary advancement |

### What got resolved
- Node 25 EOL landed on schedule (not a surprise, but CI matrix cleanup is now required).

### What got worse
- Node 20 at 32 days post-EOL with no `engines` field and no visible deprecation warning in published Nx.
- Decorators TC39 regression (Stage 3 -> Stage 2.7) is the biggest TC39 surprise in this tracking window.

### What's new since last scan
- Deno 2.8.1 patch (browser field map, BYONM bin walk, compile cache revert).
- TC39 May 2026 plenary results: 4-5 proposals advanced, `using` likely Stage 4, Decorators regression.
- Node 25 officially EOL.

---

## Delta vs June-01 scan (W24 update, 2026-06-15)

| Item | 2026-06-01 | 2026-06-15 | Change |
|------|------------|------------|--------|
| Node 20 post-EOL | 32 days | **46 days** | +14d. Still no `engines` field. **8th flag.** |
| Node 25 CI cleanup | EOL'd today | Due | Verify cleanup happened. |
| Latest Node Current | 26.2.0 | **26.3.0** | Minor: `--permission` stable, `permission.drop()`, Buffer 64KB, npm 11.16.0 |
| Latest Node LTS | 24.16.0 | 24.16.0 | No change |
| Latest Bun | 1.3.14 | 1.3.14 | No change (33-day gap -- unusually quiet) |
| Latest Deno | 2.8.1 | **2.8.3** | Two releases: PQC crypto, Jupyter rewrite, native http2 OTel, task runner env-file |
| Nx `engines` field | null (7th) | null | **8th consecutive miss** |
| Bun lockfile #16252 | dead | dead | No change |
| TC39 `using` Stage 4 | "likely" | **confirmed** | Absent from Stage 3 in two consecutive fetches |
| TC39 Decorators Stage 2.7 | regression | **holds** | No reversal in June 1-15 window |
| Upcoming Node security release | -- | **~June 17** | Blog listed. Affects v22/v24/v26. Watch for patches. |

### What got resolved
- TC39 `using` (Explicit Resource Management) Stage 4 is now confirmed -- "likely" from June-01 promoted to confirmed.

### What got worse
- Node 20 now 46 days post-EOL with no Nx `engines` field and no visible deprecation warning. 8th consecutive flag.
- `--experimental-permission` flag renamed in Node 26.3.0 -- any Nx references are now stale.

### What's new since June-01
- Node 26.3.0: `--permission` stable, `permission.drop()`, Buffer.poolSize 64 KiB, npm 11.16.0, macOS x64 Tier 2.
- Deno 2.8.2: post-quantum crypto (ML-DSA/ML-KEM), bundle compile, Jupyter JS rewrite, task env-file.
- Deno 2.8.3: compile watch mode, native http2 OTel, SubtleCrypto.supports(), Deno.readDir streaming.
- Node.js security release ~June 17 (just outside window).
