# CNW Template Health — 2026-04

_Generated: 2026-04-28 09:30 EDT (2026-W18 first weekly run; supersedes 2026-04-24 pass)_
_CNW version tested: **22.7.0** (GA released since prior 2026-04-24 scan at 22.6.5)_
_Node: v24.11.0 — npm: 11.6.1_
_Method: `npx create-nx-workspace@latest test-<name> --template=nrwl/<name>-template --nxCloud=skip --no-interactive`_

> **IMPORTANT:** This scan uses `--template=nrwl/*-template` (NOT `--preset=*-monorepo`). Templates include additional packages (notably `@nx/module-federation` in react/angular templates) that presets do not. Using `--preset` produces falsely clean results.

## Summary

Tested **4 templates** against **create-nx-workspace@22.7.0**.

| Template | Creation | 🔴 Critical | 🟠 High | 🟡 Moderate | ⚪ Low | Total |
|----------|----------|-------------|---------|-------------|--------|-------|
| `nrwl/empty-template` | OK | 0 | 0 | 6 | 0 | **6** |
| `nrwl/typescript-template` | OK | 0 | 3 | 16 | 0 | **19** |
| `nrwl/angular-template` | OK | 0 | 0 | 27 | 3 | **30** |
| `nrwl/react-template` | OK | 0 | 0 | 28 | 3 | **31** |
| **TOTAL** | — | **0** | **3** | **77** | **6** | **86** |

**Overall status:** Major moderate-count regression driven entirely by **CNW 22.7.0 GA**. The `nx >=22.7.0-beta.0` GitHub advisory now matches the installed version, surfacing the underlying `brace-expansion` (GHSA-f886-m6hf-6m8v), `follow-redirects` (GHSA-r4q5-vmmm-2653), and `yaml` (GHSA-48c2-rrv3-qjmp) advisories that were previously masked by 22.6.5 falling outside the advisory range.

- **Zero criticals** across all templates (unchanged since 2026-04-13).
- **Zero highs on angular, react, empty** (unchanged).
- **Typescript highs unchanged** at 3 (verdaccio chain: lodash, minimatch, ajv).
- **NEW (all templates):** `nx`, `@nx/workspace`, and the full transitive `@nx/*` cascade now show as moderate — purely a consequence of `nx@22.7.0` matching the `>=22.7.0-beta.0` range. **Real risk = brace-expansion DoS, follow-redirects header leak, yaml stack overflow** at the npm-internal level. No new CVEs introduced by Nx itself.
- All four templates created successfully with no deprecation warnings or peer dependency conflicts.

## Delta vs Previous Report (2026-04-24)

| Template | Prior (Apr 24, CNW 22.6.5) C/H/M/L | Current (Apr 28, CNW 22.7.0) C/H/M/L | Delta |
|----------|-------------------------------------|---------------------------------------|-------|
| empty | 0 / 0 / 0 / 0 = 0 | 0 / 0 / **6** / 0 = 6 | **+6 MODERATE** ⚠️ |
| typescript | 0 / 3 / 11 / 0 = 14 | 0 / 3 / **16** / 0 = 19 | **+5 MODERATE** ⚠️ |
| angular | 0 / 0 / 10 / 3 = 13 | 0 / 0 / **27** / 3 = 30 | **+17 MODERATE** ⚠️ |
| react | 0 / 0 / 10 / 3 = 13 | 0 / 0 / **28** / 3 = 31 | **+18 MODERATE** ⚠️ |
| **TOTAL** | 0 / 3 / 31 / 6 = 40 | 0 / 3 / **77** / 6 = 86 | **+46 MODERATE** |

### Changes since 2026-04-24

- **CNW released 22.7.0 GA.** Prior scan ran against 22.6.5; today's against 22.7.0. The `nx >=22.7.0-beta.0` advisory range now matches → `nx` core + every direct/transitive `@nx/*` dependency shows as moderate. This is a labeling shift, not a new vulnerability — the underlying issues (`brace-expansion`, `follow-redirects`, `yaml`) were already present in 22.6.5 but didn't surface because the npm advisory was tagged against the 22.7.0-beta channel.
- **`postcss <8.5.10` (GHSA-qx2v-qp2m-jg93) appears NEW in angular** — XSS via Unescaped `</style>` in CSS Stringify Output. Cascade through `@angular-devkit/build-angular` + `@analogjs/vite-plugin-angular`. Installed: `postcss@8.5.6` (top-level) + `postcss@8.5.12` (nested under `css-minimizer-webpack-plugin` — already patched). **The 8.5.6 top-level resolution is the vulnerable copy.**
- **No new advisories on react.** Increase is fully accounted for by the nx@22.7.0 cascade.
- **uuid `<14.0.0` advisory (GHSA-w5hq-g745-h8pq)** still present in angular (sockjs chain) and typescript (verdaccio/@cypress/request chain). No upstream fix yet.
- **Verdaccio chain unchanged** in typescript: 3 high (lodash, minimatch, @verdaccio/core) — same root cause since 2026-04-13.

## Results by Template

### nrwl/empty-template
- Creation: OK (no deprecations, clean install)
- npm audit: **0 critical, 0 high, 6 moderate, 0 low**
- Vulnerabilities (all transitive under `nx@22.7.0`):
  - 🟡 MODERATE `brace-expansion` 4.0.0–5.0.4 — Zero-step sequence causes process hang and memory exhaustion (GHSA-f886-m6hf-6m8v) — **transitive** (via `nx`)
  - 🟡 MODERATE `follow-redirects` <=1.15.11 — Leaks Custom Authentication Headers to Cross-Domain Redirect Targets (GHSA-r4q5-vmmm-2653) — **transitive** (via `nx`)
  - 🟡 MODERATE `yaml` 2.0.0–2.8.2 — Stack Overflow via deeply nested YAML collections (GHSA-48c2-rrv3-qjmp) — **transitive** (via `nx`)
  - 🟡 MODERATE `nx` >=22.7.0-beta.0 — depends on the three above — **direct** (top-level `nx@22.7.0`)
  - 🟡 MODERATE `@nx/workspace` >=22.7.0-beta.0 — via `nx` — **transitive**
  - 🟡 MODERATE `@nx/js` >=22.7.0-beta.0 — via `@nx/workspace` — **transitive**
- npm audit fix would downgrade nx to 22.6.5 (semver-major).

### nrwl/typescript-template
- Creation: OK (no deprecations, clean install)
- npm audit: **0 critical, 3 high, 16 moderate, 0 low**
- Vulnerabilities:
  - 🟠 HIGH `lodash` <=4.17.23 — Prototype Pollution in `_.unset`/`_.omit` (GHSA-xxjr-mmjv-4gpg, GHSA-f23m-r3pf-42rh) + Code Injection via `_.template` (GHSA-r5fr-rjxr-66jc) — **transitive** (via `verdaccio → @verdaccio/local-storage-legacy@11.1.1`; vendored copy `lodash@4.17.21`)
  - 🟠 HIGH `minimatch` 7.0.0–7.4.7 — ReDoS (GHSA-3ppc-4f35-3m26, GHSA-7r86-cg39-jmmj, GHSA-23c5-xmqv-rm74) — **transitive** (via `@verdaccio/core@8.0.0-next-8.21`; nested 7.4.6/7.4.9)
  - 🟠 HIGH `@verdaccio/core` <=8.0.0-next-8.30 — depends on vulnerable `ajv` + `minimatch` — **transitive**
  - 🟡 MODERATE `ajv` 7.0.0-alpha.0–8.17.1 — ReDoS via `$data` (GHSA-2g4f-4pwh-qvx6) — **transitive** (via `@verdaccio/core`)
  - 🟡 MODERATE `@verdaccio/local-storage-legacy` — via `@verdaccio/core` + `lodash` — **transitive**
  - 🟡 MODERATE `verdaccio` 5.29.0–7.0.0-beta.6 — **direct** (`verdaccio@6.4.0`)
  - 🟡 MODERATE `uuid` <14.0.0 — Missing buffer bounds check (GHSA-w5hq-g745-h8pq) — **transitive** (via `verdaccio → @cypress/request → uuid@8.3.2`)
  - 🟡 MODERATE `@cypress/request` — via `uuid` — **transitive**
  - 🟡 MODERATE `brace-expansion`, `follow-redirects`, `yaml` (the nx cascade) — **transitive** (via `nx@22.7.0`)
  - 🟡 MODERATE `nx` + `@nx/workspace` + `@nx/js` + `@nx/eslint` + `@nx/eslint-plugin` + `@nx/vite` + `@nx/vitest` + `@nx/web` — cascade — mix of **direct/transitive**

  **Root cause unchanged:** Verdaccio (local npm registry for e2e testing) pulls stale `lodash`, `minimatch`, `ajv`, plus the now-vulnerable `uuid` via `@cypress/request`. No mitigation published upstream.

### nrwl/angular-template
- Creation: OK (no deprecations, clean install)
- npm audit: **0 critical, 0 high, 27 moderate, 3 low**
- New this scan:
  - 🟡 MODERATE `postcss` <8.5.10 — XSS via Unescaped `</style>` in CSS Stringify Output (GHSA-qx2v-qp2m-jg93) — **transitive** (via `@angular-devkit/build-angular`). Installed top-level: `postcss@8.5.6` (vulnerable). Nested copy under `css-minimizer-webpack-plugin@8.5.12` is patched.
  - 🟡 MODERATE `nx`/`@nx/workspace` cascade across full `@nx/*` graph (`@nx/angular`, `@nx/esbuild`, `@nx/eslint`, `@nx/eslint-plugin`, `@nx/jest`, `@nx/js`, `@nx/module-federation`, `@nx/node`, `@nx/playwright`, `@nx/rspack`, `@nx/vite`, `@nx/vitest`, `@nx/web`, `@nx/webpack`) — all transitive labeling under `nx@22.7.0`
- Persisting from prior scan:
  - 🟡 MODERATE `uuid` <14.0.0 (GHSA-w5hq-g745-h8pq) — **transitive** (via `sockjs@0.3.24 → uuid@8.3.2`). npm audit notes **"No fix available"**.
  - 🟡 MODERATE `sockjs` >=0.3.17 — via `uuid` — **transitive**
  - 🟡 MODERATE `@rspack/dev-server` 1.2.0–1.2.1 — via sockjs — **transitive**
  - 🟡 MODERATE `webpack-dev-server` >=2.0.0-beta — via sockjs — **transitive**
  - 🟡 MODERATE `@angular-devkit/build-angular` — via `@angular-devkit/build-webpack` + webpack-dev-server + postcss — **direct**
  - 🟡 MODERATE `@angular-devkit/build-webpack` — via webpack-dev-server — **transitive**
  - 🟡 MODERATE `@analogjs/vite-plugin-angular` <=0.2.12 || >=1.4.0-beta.1 — **direct**
  - 🟡 MODERATE `brace-expansion`, `follow-redirects`, `yaml` (nx cascade) — **transitive**
  - ⚪ LOW `@tootallnate/once`, `http-proxy-agent`, `jsdom` — unchanged 3-low chain (via `jsdom@22.1.0`)
- Installed versions (vite/Angular ranges held since 2026-04-22): `@angular/platform-server@21.2.9`, `@angular/build@21.2.7`, `@angular/core@21.2.9`, `vite@7.3.2`, `webpack-dev-server@5.2.3`, `@rspack/dev-server@1.2.1`, `postcss@8.5.6`.

### nrwl/react-template
- Creation: OK (no deprecations, clean install)
- npm audit: **0 critical, 0 high, 28 moderate, 3 low**
- New this scan:
  - 🟡 MODERATE `nx`/`@nx/workspace` cascade across `@nx/*` graph (incl. `@nx/react`, `@nx/esbuild`, `@nx/eslint`, `@nx/eslint-plugin`, `@nx/jest`, `@nx/js`, `@nx/module-federation`, `@nx/node`, `@nx/playwright`, `@nx/rollup`, `@nx/vite`, `@nx/vitest`, `@nx/web`) — transitive labeling under `nx@22.7.0`
  - 🟡 MODERATE `brace-expansion`, `follow-redirects`, `yaml` (nx cascade) — **transitive**
- Persisting from prior scan:
  - 🟡 MODERATE `file-type` 13.0.0–21.3.1 — infinite loop in ASF parser (GHSA-5v7r-6r5c-r473) + ZIP Decompression Bomb DoS (GHSA-j47w-4g3g-c36v) — **transitive** (via `@swc/cli@0.7.10 → @xhmikosr/*` chain)
  - 🟡 MODERATE `@xhmikosr/archive-type`, `@xhmikosr/bin-wrapper`, `@xhmikosr/decompress`, `@xhmikosr/decompress-tar`, `@xhmikosr/decompress-tarbz2`, `@xhmikosr/decompress-targz`, `@xhmikosr/decompress-unzip`, `@xhmikosr/downloader` — all cascade from `file-type` — **transitive**
  - 🟡 MODERATE `@swc/cli` 0.5.1–0.8.0 — **direct** (`@swc/cli@0.7.10`)
  - ⚪ LOW `@tootallnate/once`, `http-proxy-agent`, `jsdom` — unchanged (via `jsdom@22.1.0`)
- Note: top-level `vite@8.0.10` (outside all vite advisory ranges); `vitest` pulls a nested `vite@7.3.2`. React template doesn't pull `sockjs`/`webpack-dev-server`/`verdaccio`, so it skips both the angular sockjs cluster and the typescript verdaccio cluster.

## Cross-Template Analysis

### Shared across ALL FOUR templates (NEW this cycle — nx@22.7.0 cascade)
Driven entirely by `nx >=22.7.0-beta.0` advisory range now matching GA 22.7.0:
- `brace-expansion` (GHSA-f886-m6hf-6m8v)
- `follow-redirects` (GHSA-r4q5-vmmm-2653)
- `yaml` (GHSA-48c2-rrv3-qjmp)
- `nx` (top-level)
- `@nx/workspace`, `@nx/js` (transitive cascade)

These three underlying advisories are addressable via simple `npm audit fix` (without `--force`) — but the proposed fix downgrades `nx` to 22.6.5 (semver-major). Real fix requires Nx to bump `brace-expansion`, `follow-redirects`, `yaml` in the next nx release.

### Shared across angular + typescript (uuid GHSA-w5hq-g745-h8pq, no upstream fix)
- **Typescript:** `verdaccio → @cypress/request → uuid@8.3.2` (+2 moderate)
- **Angular:** `webpack-dev-server → sockjs → uuid@8.3.2` and parallel `@rspack/dev-server → sockjs → uuid@8.3.2` (+10 moderate cascade incl. postcss)

### Shared across angular + react (low severity, unchanged)
- `@tootallnate/once` / `http-proxy-agent` / `jsdom` chain (3 low each) — from shared `jsdom~22.1.0` dev dep.

### Template-specific
- **typescript only:** verdaccio chain — 3 highs (lodash, minimatch, @verdaccio/core).
- **react only:** `file-type` / `@xhmikosr/*` chain via `@swc/cli` (10 moderate).
- **angular only:** sockjs/webpack-dev-server/postcss cluster.
- **empty:** clean apart from the nx cascade.

## Action Items

### Fix ASAP (Critical)
None. Zero critical vulnerabilities across all templates.

### Resolve This Week (High)

1. **verdaccio chain in typescript template** (3 high, ongoing since 2026-04-13) — Verdaccio 6.4.0 pulls stale `lodash@4.17.21` (vendored), `minimatch@7.4.6/7.4.9` (nested), and `@verdaccio/core@8.0.0-next-8.21`. Options unchanged:
   - (a) wait for verdaccio to publish a release shipping updated `@verdaccio/core` (not yet available)
   - (b) add `overrides` in the template package.json (`"overrides": { "lodash": "^4.17.24", "minimatch": "^9.0.0", "ajv": "^8.18.0" }`)
   - (c) remove verdaccio from the default typescript template if only used for opt-in e2e testing

### Noted (Medium/Low — no immediate action)

2. **NEW: nx@22.7.0 surfaces brace-expansion / follow-redirects / yaml across all templates (+46 moderate aggregate)** — Pure labeling effect of the GA release matching the existing advisory range. Real fix is in Nx core: bump `brace-expansion >=2.0.2`, `follow-redirects >=1.15.12`, `yaml >=2.9.0` in nx's nested `node_modules`. Suggests a `chore(core)` or release-bundle PR to refresh nx's transitive versions before next patch. Low real-world exploitability (CLI process, not a server).
3. **NEW (angular): postcss@8.5.6 top-level via @angular-devkit/build-angular** — XSS in CSS Stringify Output (GHSA-qx2v-qp2m-jg93). Fix requires `postcss >=8.5.10`. Angular devkit needs to bump or Nx can pin via override.
4. **uuid@8.3.2 / sockjs cluster (angular, +10 moderate)** — GHSA-w5hq-g745-h8pq. No upstream sockjs fix yet. Buffer-bounds-check only impacts `uuid.v3/v5/v6` calls with a provided `buf`; sockjs uses uuid for session IDs without `buf`, so practical risk is low.
5. **uuid@8.3.2 / @cypress/request (typescript, +2 moderate)** — Same advisory, different path. Mitigation tied to the verdaccio strategy above.
6. **file-type / @xhmikosr/* chain in react (10 moderate, unchanged)** — Cascades from `@swc/cli`'s binary-download helper. Low real-world exploitability (requires adversarial archives at build time). Monitor `@swc/cli` for upstream fix.
7. **@tootallnate/once chain (low, angular + react)** — Via `jsdom`. Upstream jsdom upgrade path is breaking (22.x → 29.x). Low risk.

### Monitoring

- **Nx core transitive refresh:** track when nx publishes a release with bumped `brace-expansion`/`follow-redirects`/`yaml` — would zero out the nx-cascade contribution across all templates.
- **uuid@14 adoption:** watch sockjs, @cypress/request for adopters; would close two clusters.
- **verdaccio modernization:** watch for `verdaccio@7` GA shipping updated `@verdaccio/core` with patched lodash/minimatch/ajv.
- **postcss@8.5.10+ in @angular-devkit/build-angular:** watch Angular CLI release notes for the bump.

---

*Next scan: 2026-W19 (or after next create-nx-workspace patch release).*
*Prior 2026-W17 results (CNW 22.6.5 baseline) preserved in delta tables above.*
