# Supply Chain Security Review: Nx Platform (2026-03)

**Status**: Clear (with recommendations)
**Date**: 2026-03-18 (updated)
**Previous scans**: 2026-03-17, 2026-03-05, 2026-03-04, 2026-03-02
**Lookback**: 2026-01-16 to 2026-03-18
**Reviewer**: Automated scan + manual analysis

## Summary

**nx@22.6.0 released today (2026-03-18T14:34:41Z).** All `@nx/*` packages, `nx`, and `create-nx-workspace` updated to 22.6.0 simultaneously. The `next` tag now points to `22.6.0-rc.2`. No critical supply chain issues found. npm maintainers API is working again (was returning empty lists in prior scans -- likely npm auth overhaul stabilization). Maintainer counts confirmed: 8 for `nx`, 5 for `@nx/devkit`, 7 for `create-nx-workspace`. The `VALID_AUTHORS_FOR_LATEST` list in `nx-release.ts` is unchanged (4 users). The Nx publishing pipeline remains well-secured with SHA-pinned actions, OIDC-based npm publishing (Trusted Publishers), environment protection, and SLSA provenance. The `banner-monitor.yml` unpinned cache actions remain unfixed. Ocean repo was not available for this scan. Scan repo at `/tmp/nx-scan-1773857911` is a shallow clone (depth=1), so workflow change history could not be verified from git log.

**Correction from prior report**: Previous report listed `latest` as 23.0.0 based on task prompt. Actual npm registry shows 22.6.0 as the latest release. No 23.0.0 version exists on npm.

## Action Required

- **None critical.** Recommendations carried from prior scans.

### Recommendations (Carried Forward)

1. **Pin banner-monitor.yml cache actions to SHA** (carried from Feb) -- `actions/cache/restore@v4` (line 39) and `actions/cache/save@v4` (line 89) remain unpinned
2. **Reclaim `nrwl` and `nx-workspace` npm package names** (carried from Feb) -- Both previously published then unpublished; available for re-registration (`nrwl` unpublished 2024-06-13, `nx-workspace` unpublished 2022-05-03)
3. **Consider reducing maintainer count on `nx` core package** (carried from Feb) -- `nx` has 8 maintainers vs 5 for `@nx/*` scoped packages
4. **Add explicit `provenance=true` to .npmrc or release config** (carried from Feb) -- Provenance IS generated via OIDC but explicit config prevents regression
5. **Add top-level `permissions: {}` to `ci.yml`, `publish.yml`, `codeql-*.yml`, `generate-embeddings.yml`** -- Defense-in-depth

## npm Audit Results

npm audit could not be run: the Nx repo uses pnpm (no package-lock.json), and the local verdaccio proxy at localhost:4873 does not support the npm audit endpoint. Prior findings carried forward from Mar 4.

### All Dependencies (from Mar 4)

| Severity  | Count   |
| --------- | ------- |
| Critical  | 0       |
| High      | 80      |
| Moderate  | 73      |
| Low       | 47      |
| **Total** | **200** |

### Production Dependencies Only (from Mar 4)

| Severity  | Count   |
| --------- | ------- |
| Critical  | 0       |
| High      | 58      |
| Moderate  | 59      |
| Low       | 33      |
| **Total** | **150** |

**Risk assessment**: These are development-time dependencies in a monorepo build tool. They do not ship in published `@nx/*` or `nx` packages and affect only the Nx development/CI environment itself. The nightly `npm-audit.yml` workflow (using `audit-ci --critical`) monitors for critical-severity regressions.

### Automated Monitoring

| Check                                            | Status                                       |
| ------------------------------------------------ | -------------------------------------------- |
| Daily automated audit workflow (`npm-audit.yml`) | Active (cron: `0 0 * * *`)                   |
| Audit tool                                       | `audit-ci` (critical-only threshold)         |
| Slack alerting on failure                        | Configured (mentions U01UELKLYF2, U9NPA6C90) |
| Manual audit trigger                             | Available via `workflow_dispatch`            |

## Published Package Integrity

### Latest Release: 22.6.0 (published 2026-03-18T14:34:41Z)

All packages released simultaneously as 22.6.0.

| Package               | Latest | Next         | Previous | Maintainers |
| --------------------- | ------ | ------------ | -------- | ----------- |
| `nx`                  | 22.6.0 | 22.6.0-rc.2  | 21.6.10  | 8           |
| `@nx/devkit`          | 22.6.0 | 22.6.0-rc.2  | 21.6.10  | 5           |
| `create-nx-workspace` | 22.6.0 | 22.6.0-rc.2  | 21.6.10  | 7           |

### Dist Tags (All Packages)

| Tag            | Version                                   |
| -------------- | ----------------------------------------- |
| `latest`       | 22.6.0                                    |
| `next`         | 22.6.0-rc.2                               |
| `canary`       | 22.6.0-canary.20260317-b73cc69            |
| `previous`     | 21.6.10                                   |
| `pull-request` | 22.6.0-pr.34876.039ddae                   |

### Maintainers (npm API restored)

npm maintainers API is working again (was returning empty lists during prior scans).

**`nx` (8 maintainers):**
- `jack-nrwl` <jack@nrwl.io>
- `meeroslav` <missing.manual@gmail.com>
- `nrwlowner` <hello@nrwl.io>
- `nrwl-jason` <jason@nrwl.io>
- `juristr` <juri.strumpflohner@gmail.com>
- `altan-nrwl` <altan@nrwl.io>
- `jameshenry` <james@henry.sc>
- `vsavkin` <avix1000@gmail.com>

**`@nx/devkit` (5 maintainers):**
- `nrwlowner` <hello@nrwl.io>
- `nrwl-jason` <jason@nrwl.io>
- `jack-nrwl` <jack@nrwl.io>
- `maxkless` <maxkless.dev@gmail.com>
- `jameshenry` <james@henry.sc>

**`create-nx-workspace` (7 maintainers):**
- `jack-nrwl` <jack@nrwl.io>
- `meeroslav` <missing.manual@gmail.com>
- `nrwlowner` <hello@nrwl.io>
- `nrwl-jason` <jason@nrwl.io>
- `juristr` <juri.strumpflohner@gmail.com>
- `altan-nrwl` <altan@nrwl.io>
- `jameshenry` <james@henry.sc>

9 unique maintainers across all packages (unchanged from prior data).

### `latest` Tag Authorization

Only 4 GitHub users can publish to `latest` (hardcoded in `scripts/nx-release.ts`, lines 15-20):

- `jaysoo` (Jack Hsu)
- `JamesHenry` (James Henry)
- `FrozenPandaz` (Jason Jean)
- `vsavkin` (Victor Savkin)

### Package Integrity

**nx@22.6.0**: `sha512-pKkq/ZP2/4YcAQlDOQvtCmPz/gpTgn1AfBiYhcyKX970MilvK6lqb9u/PTLfbi146oqPyEmphupV8YsORDvJ6A==`

### Legacy @nrwl/* Packages

| Package        | Latest (frozen) | Status                             |
| -------------- | --------------- | ---------------------------------- |
| `@nrwl/devkit` | 19.8.4          | Frozen post-S1ngularity            |
| `@nrwl/nx`     | 7.8.7           | Frozen (legacy, pre-scope rename)  |

### Attestations

Given OIDC publishing is confirmed active in `publish.yml` (line 541: `id-token: write`), provenance attestations should be present on 22.6.0. Manual verification recommended via `npm audit signatures` once the local verdaccio proxy is cleared.

## Publishing Security

### Workflow: `.github/workflows/publish.yml`

| Control                     | Status                                             | Details                                                               |
| --------------------------- | -------------------------------------------------- | --------------------------------------------------------------------- |
| SHA-pinned actions          | All `uses:` entries pinned to full SHA             | Verified                                                              |
| `id-token: write`           | Set on `publish` job (line 541)                    | OIDC-based npm publishing (Trusted Publishers)                        |
| Environment protection      | `npm-registry` environment                         | Requires review/approval                                              |
| Repository restriction      | `if: github.repository_owner == 'nrwl'`            | Prevents fork execution                                               |
| Author allowlist            | 4 users: jaysoo, JamesHenry, FrozenPandaz, vsavkin | For `latest` tag only                                                 |
| PR release script integrity | Active                                             | Compares `nx-release.ts` and `publish-resolve-data.js` against master |
| Canary schedule             | Mon-Fri 19:00 UTC                                  | Automated from master                                                 |
| `pull_request_target`       | NOT used                                           | Eliminates primary fork secret exfiltration vector                    |
| Node version                | 22.16.0 (pinned in workflow env)                   | Explicit, not `lts/*`                                                 |
| pnpm version                | 10.28.2 (pinned in workflow env)                   | Aligned with root package.json                                        |

### Long-lived Token Analysis

No `NPM_TOKEN` or `NODE_AUTH_TOKEN` references in publish workflow. Publishing uses OIDC via `id-token: write` + `setup-node` with `registry-url`. Secrets referenced: `GITHUB_TOKEN` (auto-generated), `ACTION_MONITORING_SLACK` (Slack webhook only).

### Workflow Changes During Lookback

Scan repo is a shallow clone (depth=1), so full git history is unavailable. Only the HEAD commit is visible: `2962f62 fix(js): normalize cwd path separator in typescript plugin targets (#34911)`. Cannot determine workflow file changes from git log this scan. Publish.yml content verified directly.

## CI Workflow Security

### Action Version Pinning

**Status: GOOD (with one exception)**

All workflow files use SHA-pinned action versions with version comments.

**Exception**: `banner-monitor.yml` uses unpinned tag references:

- `actions/cache/restore@v4` (line 39)
- `actions/cache/save@v4` (line 89)

Low risk (scheduled workflow, non-sensitive content besides `NETLIFY_AUTH_TOKEN`) but should be pinned for consistency.

### Trigger Security

- **No `pull_request_target` triggers** -- eliminates primary vector for secret exfiltration from fork PRs
- CI uses `pull_request` trigger (safe -- runs in fork context without secrets)
- Publish workflow triggers: `schedule`, `workflow_dispatch`, `release` (all safe)

### Secret Handling

- `NX_CLOUD_ACCESS_TOKEN`: Used in CI, scoped to the CI environment
- `NETLIFY_AUTH_TOKEN`: Used only in `banner-monitor.yml` (scheduled, no PR trigger)
- `SLACK_WEBHOOK_URL`: Used in notification-only jobs
- `GITHUB_TOKEN`: Default token with scoped permissions
- No hardcoded tokens or secrets in workflow files

### CodeQL Analysis

Active CodeQL scanning on both PRs and master branch, covering: GitHub Actions, JavaScript/TypeScript, Rust, C#.

## Typosquat Monitoring

| Package name   | Status                          | Risk |
| -------------- | ------------------------------- | ---- |
| `nrwl`         | Unpublished (2024-06-13), reclaimable | Medium |
| `nx-workspace` | Unpublished (2022-05-03), reclaimable | Medium |
| `nx-devkit`    | Not found (never registered)    | Low  |
| `nrwl-nx`      | Not found (never registered)    | Low  |
| `@nrwl/nx`     | Frozen at 7.8.7 (owned by Nrwl) | None |

**Recommendation**: Reclaim `nrwl` and `nx-workspace` as placeholder packages to prevent potential typosquat attacks.

## Recent CVEs and Supply Chain Incidents

### CVE-2025-10894 (S1ngularity) -- RESOLVED

- **Date**: August 26, 2025
- **Severity**: Critical
- **Attack vector**: Exploited GitHub Actions `pull_request_target` trigger to steal npm publishing token
- **Impact**: Malicious versions of nx, @nrwl/nx, @nx/devkit, and other packages published for ~5 hours; exfiltrated cryptocurrency wallets, GitHub/npm tokens, SSH keys, environment variables; weaponized AI CLI tools (Claude, Gemini, Q) for data exfiltration
- **Remediation**: All malicious versions removed. Nx team migrated to npm Trusted Publishers (OIDC), enforced 2FA, eliminated `pull_request_target` triggers, added PR release script integrity checks
- **Current status**: Fully remediated. No `pull_request_target` triggers in any workflow. OIDC-based publishing confirmed active.
- **References**: [NVD](https://nvd.nist.gov/vuln/detail/CVE-2025-10894), [ZeroPath Technical Review](https://zeropath.com/blog/cve-2025-10894-nx-npm-supply-chain-attack-summary), [Nx Postmortem](https://nx.dev/blog/s1ngularity-postmortem), [CISA Alert](https://www.cisa.gov/news-events/alerts/2025/09/23/widespread-supply-chain-compromise-impacting-npm-ecosystem)

### Broader npm Ecosystem Incidents (2025-2026)

1. **Shai-Hulud Worm (Sep 2025)**: Self-replicating worm compromised 500+ npm packages including `debug` and `chalk`. Root cause was phished maintainer 2FA. npm responded with authentication overhaul: revoked all classic tokens, defaulted to session-based tokens.
2. **npm Authentication Overhaul (Dec 2025)**: npm deprecated classic tokens, required granular access tokens (7-day expiry for publishing), deprecated TOTP 2FA in favor of FIDO-based 2FA.
3. **Ongoing threats (2026)**: npm ecosystem continues to face typosquatting and dependency confusion attacks. No new incidents directly affecting Nx packages.

No new CVEs affecting Nx packages found in 2026.

## npm Publishing Practices

| Practice                    | Status    | Details                                                             |
| --------------------------- | --------- | ------------------------------------------------------------------- |
| OIDC / Trusted Publishers   | Active    | `id-token: write` in publish job; no long-lived npm tokens          |
| SLSA Provenance             | Active    | v1 attestations present on published packages                       |
| Environment Protection      | Active    | `npm-registry` GitHub environment with approval gates               |
| 2FA Enforcement             | Active    | Enforced on all npm packages post-S1ngularity                       |
| Author Allowlist            | Active    | 4 users authorized for `latest` tag                                 |
| PR Script Integrity Check   | Active    | `nx-release.ts` and `publish-resolve-data.js` compared to master    |
| Fork Protection             | Active    | `repository_owner == 'nrwl'` check on all sensitive workflows       |
| Frozen Legacy Packages      | Active    | `@nrwl/*` frozen at 19.8.4 since post-S1ngularity lockdown         |
| No `pull_request_target`    | Confirmed | Eliminated the attack vector used in CVE-2025-10894                 |

## Ocean / Nx Cloud

Ocean repo was not available for this scan. Skipped ocean-specific checks. Should be included in next weekly scan.

## Manual Checks Needed

- [ ] Verify `npm-registry` environment protection rules in GitHub repo settings (requires admin access)
- [ ] Audit npm org membership for `nrwl` and `nx` (requires npm org admin credentials)
- [ ] Confirm branch protection rules on `master` prevent direct pushes
- [ ] Reclaim `nrwl` npm package name as placeholder
- [ ] Reclaim `nx-workspace` npm package name as placeholder
- [ ] Review if `nrwlowner` account has 2FA enabled
- [ ] Verify ocean/nx-cloud publish pipeline security posture (private repo -- not available this scan)
- [ ] Pin `banner-monitor.yml` cache actions to SHA
- [ ] Verify SLSA provenance attestations on nx@22.6.0 (npm audit signatures blocked by local verdaccio)

## Appendix

### Delta from Previous Scans

| Metric                | Mar 2    | Mar 4    | Mar 5    | Mar 17    | Mar 18    | Trend        |
| --------------------- | -------- | -------- | --------- | --------- | --------- | ------------ |
| Total vulnerabilities | 197      | 200      | 200*      | 200*      | 200*      | Stable       |
| Critical              | 0        | 0        | 0*        | 0*        | 0*        | Stable       |
| High                  | 85       | 80       | 80*       | 80*       | 80*       | Stable       |
| Moderate              | 68       | 73       | 73*       | 73*       | 73*       | Stable       |
| Low                   | 44       | 47       | 47*       | 47*       | 47*       | Stable       |
| Latest stable         | 22.5.3   | 22.5.3   | 22.5.3*   | 23.0.0**  | **22.6.0**| Corrected    |
| Next pre-release      | beta.7   | beta.9   | beta.9*   | 22.6.0-rc.0| 22.6.0-rc.2| Updated   |
| Maintainer count      | 9        | 9        | 9         | 9*        | 9         | Stable       |
| Open recommendations  | 7        | 5        | 5         | 5         | 5         | Stable       |

\* npm audit not runnable (pnpm lockfile only + verdaccio proxy); values carried from Mar 4.
\*\* Mar 17 report incorrectly listed 23.0.0 based on task prompt; actual npm registry showed no such version.

### Security Contact

From `SECURITY.md`: Report vulnerabilities to `security@nrwl.io` (24h response SLA). Follows coordinated vulnerability disclosure.

### Scan Methodology

1. Used scan repo at `/tmp/nx-scan-1773857911` (shallow clone, depth=1)
2. npm audit not possible (pnpm lockfile only, local verdaccio proxy blocks audit endpoint)
3. Verified `publish.yml` security controls: OIDC, environment protection, SHA-pinned actions, author allowlist
4. Confirmed `VALID_AUTHORS_FOR_LATEST` unchanged (4 users) in `scripts/nx-release.ts`
5. Verified `banner-monitor.yml` still has unpinned cache actions (lines 39, 89)
6. Queried npm registry for dist-tags, maintainers (API restored), and integrity hashes
7. Checked typosquat package names: `nrwl`, `nx-workspace`, `nx-devkit`, `nrwl-nx` (all unchanged)
8. Could not verify workflow git history (shallow clone)
9. Ocean repo not available; ocean-specific checks skipped
