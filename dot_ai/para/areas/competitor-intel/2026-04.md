# Competitor Intelligence -- April 2026

_Scan date: 2026-04-20 (updated from 2026-04-16)_
_Period: 2026-02-19 -- 2026-04-20 (60-day lookback)_
_Previous report: [2026-03.md](./2026-03.md)_

## Executive Summary

- **Turborepo**: v2.9.6 still the latest stable (Apr 10). Canary train extended to v2.9.7-canary.13 (Apr 16) -- all recent canary traffic is **Vercel auth plumbing** (OAuth device flow, legacy-token compat, cross-flow token recovery) and **PTY graceful-shutdown fixes**. No new features since 2026-04-16; cadence is slowing into fix/compat mode prior to the v2.9.7 stable cut. ([blog](https://turborepo.dev/blog/2-9), [v2.9.0](https://github.com/vercel/turborepo/releases/tag/v2.9.0))
- **Moon** kept iterating: **v2.2.1 (Apr 16)** and **v2.2.2 (Apr 20)** are follow-up patches to v2.2.0. Notables: async affected tracker +5-10% (v2.2.1), pnpm v10 multi-doc lockfile parsing fix (v2.2.1), remote cache error treated as miss (v2.2.1), request retries via proto (v2.2.1), plugin-loading perf regression fix and VS Code extension v2-schema update (v2.2.2). Core AI Skills / daemon story unchanged from prior scan. ([v2.2.1](https://github.com/moonrepo/moon/releases/tag/v2.2.1), [v2.2.2](https://github.com/moonrepo/moon/releases/tag/v2.2.2))
- **Bazel 9.0.2 (April 9) stable** plus **9.1.0rc1/rc2/rc3 (Apr 9/14/15)** -- 9.1 introduces `--experimental_remote_cache_chunking` and `--rewind_lost_inputs` (rerun actions to recover from cache eviction mid-build). rc2 added a **CcInfo-removal compat warning** for `rules_go`/`rules_nodejs` consumers. No new RC since Apr 15. ([9.1.0rc3](https://github.com/bazelbuild/bazel/releases/tag/9.1.0rc3))
- **Gradle 9.5.0-RC3 (Apr 17) -- NEW this scan.** Gradle's 9.5 RC train advanced from RC2 (Apr 9) to RC3 in 8 days. Headline features: **task provenance in reports and failure messages** (surfaces which script/plugin registered a failing task -- a debuggability feature that encroaches on Nx Cloud's task-analytics framing), **precompiled Kotlin Settings plugins with type-safe accessors**, **wrapper download retries**, new `--develocity-url` CLI flag for automatic Build Scan publishing, `disallowChanges()` for plugin collections. No AI features in 9.5. ([9.5.0-RC3](https://github.com/gradle/gradle/releases/tag/v9.5.0-RC3))
- **Pants accelerated (April 15-16)**: shipped `release_2.30.2` stable, `release_2.31.1rc0`, and new `release_2.32.0a0` alpha channel (formalized via new GitHub Actions workflow). 2.32 dev branch continues uv-PEX, rust port of scalar fields, Python 3.14 devcontainer. First **alpha-release** channel for Pants -- they are formalizing pre-release cadence. Unchanged since 2026-04-16 scan.
- **Threat level: HIGH -> HIGH (unchanged).** Turborepo's 2.9 launch still defines the threat picture -- cadence is slowing into bug-fix/OAuth work without new strategic features in the last 4 days. Moon continues to harden v2.2. The "2 of 3 direct competitors now have explicit agent surfaces" reading from Apr 16 stands unchanged. Gradle 9.5 RC3 adds **task provenance** as a new debuggability vector worth tracking but it's JVM-scoped and low direct threat to Nx.
- **Cross-competitor theme: "agent-first surfaces" broadened from 1 to 2 explicit competitors.** Turbo has `turbo query` + Agent Skills + `@vercel/agent-readability` + cursor[bot]; Moon has an AI skills system (`debug-task`). No new agent-facing product surface from Bazel, Gradle, or Pants this cycle.

## Delta vs March Report

| Signal | March | April | Change |
|---|---|---|---|
| Turbo latest stable | v2.8.21 (Mar 28) | **v2.9.6** (Apr 10) | +7 stable releases in 13 days |
| Turbo 2.9 blog | flagged as canary | **shipped Mar 25** | marketing event landed |
| `experimentalCI` key | canary.3 | shipped in v2.9.0 | moved to stable |
| `--affected` + `--filter` compose | not available | **shipped v2.9.4** | new gap to verify |
| `cacheMaxAge`/`cacheMaxSize` | not present | **shipped v2.9.1** | local cache eviction policy |
| Moon release cadence | v2.1.3 (Mar 26) | **v2.2.0 / v2.2.1 / v2.2.2 (Apr 12/16/20)** | major minor bump + 2 patches |
| Moon daemon | absent | **v2.2.0 unstable** | strategic convergence |
| Bazel | 9.0.1 (Mar 10) | 9.0.2 + 9.1.0rc1-rc3 | LTS patch + new minor RC advancing |
| Gradle | 9.4.1 (Mar 19), 9.5.0-rc2 flagged | **9.4.1 stable, 9.5.0-RC3 (Apr 17)** | 9.5 RC train re-advanced |
| Pants | 2.32.0.dev6 (Mar 23) | 2.32.0.dev7 (Apr 1), 2.30.2 stable, 2.31.1rc0, 2.32.0a0 | cadence accelerated, new alpha channel |

## Delta vs 2026-04-16 Scan (4-day window)

| Signal | Apr 16 | Apr 20 | Change |
|---|---|---|---|
| Turbo stable | v2.9.6 | v2.9.6 (unchanged) | No new stable; 10 days since last stable |
| Turbo canary | v2.9.7-canary.12 | **v2.9.7-canary.13** | +1 canary (auth token recovery fix) |
| Turbo canary themes | OAuth feat + pnpm v11 fix | **OAuth compat / PTY shutdown only** | cadence cooled; all auth-chain fixes |
| Moon stable | v2.2.0 | **v2.2.1 (Apr 16) + v2.2.2 (Apr 20)** | 2 patches: async tracker +5-10%, pnpm v10 multi-doc lockfile fix, remote-cache-miss-on-error, plugin load regression fix |
| Moon AI Skills | `debug-task` shipped | unchanged | no new skills yet |
| Bazel | 9.1.0rc3 (Apr 15) | unchanged | no new RC in 5 days |
| Gradle | 9.4.1 stable, no 9.5 visible | **9.5.0-RC3 (Apr 17) new** | task provenance in reports/failures + precompiled Kotlin Settings plugins + wrapper retries + `--develocity-url` CLI |
| Pants | 2.32.0a0 (Apr 16) | unchanged | no new alpha/rc |

## Turborepo

**Latest stable**: v2.9.6 (2026-04-10) -- npm `turbo@latest` = 2.9.6 (verified 2026-04-20)
**Canary**: v2.9.7-canary.13 (2026-04-16)
**Major marketing event**: [Turborepo 2.9 blog post, 2026-03-25](https://turborepo.dev/blog/2-9)

### Released this period (since 2026-02-19)

All stables: v2.8.17, v2.8.18, v2.8.19, v2.8.20, v2.8.21, **v2.9.0** (Mar 30), v2.9.1 (Mar 30), v2.9.2 (Mar 31), v2.9.3 (Mar 31), v2.9.4 (Apr 5), **v2.9.5** (Apr 7), **v2.9.6** (Apr 10). Plus canary train extended to v2.9.7-canary.13 (Apr 16). **No new stable in 10 days** -- canary cadence is in OAuth-migration finishing mode. Source: [gh release list](https://github.com/vercel/turborepo/releases).

### New since 2026-04-16 scan

- **v2.9.7-canary.13** (Apr 16): `fix: Recover Vercel auth tokens across login flows` ([PR #12631](https://github.com/vercel/turborepo/pull/12631)). Third auth-related canary in a row (canary.11 OAuth feat, canary.12 legacy Vercel auth compat, canary.13 token recovery). **Signal**: Vercel is hardening the `turbo login` OAuth migration across edge cases before cutting v2.9.7 stable. No new features.
- **v2.9.7-canary.12** (Apr 16): `fix: Preserve legacy Vercel auth compatibility`, plus an [Ultracite example](https://github.com/vercel/turborepo/pull/12615) added to examples.
- Canary cadence: +6 canaries in the 3 days Apr 13-16, then +1 canary in the 4 days Apr 16-20. The burst is cooling.

Earlier v2.8.x stables were covered in [2026-03.md](./2026-03.md). This report focuses on **2.9.x**.

### Key changes in 2.9.x

#### Features (v2.9.0 marketing highlights)

- **Time to First Task: 80-96% faster** across tested repos (Vercel backend 91%, Vercel frontend 81%, create-turbo 80%). Source: [blog](https://turborepo.dev/blog/2-9). This is startup overhead, not cache/execution speed -- but it's the number they'll market.
- **`turbo query` promoted to stable** with two first-class subcommands: `turbo query affected` (structured JSON for CI) and `turbo query ls`. GraphQL against the Package and Task Graph. They are explicitly positioning this as agent-friendly: "coding agents are incredibly adept at writing queries."
- **Circular package-dependency tolerance** (v2.9.0). Turborepo now validates the Task Graph instead of the Package Graph, allowing package-level cycles. Pitched as an adoption aid for monorepos that can't fix cycles up front.
- **`cacheMaxAge` and `cacheMaxSize`** for local cache eviction (v2.9.1, [PR #12487](https://github.com/vercel/turborepo/pull/12487)). Closes a real gap.
- **Incremental task caching** (v2.9.4, [PR #12531](https://github.com/vercel/turborepo/pull/12531)).
- **Circular package dependency detection in boundaries** (v2.9.5, [PR #12567](https://github.com/vercel/turborepo/pull/12567)).
- **`--affected` and `--filter` can be combined** (v2.9.4, [PR #12543](https://github.com/vercel/turborepo/pull/12543)). Closes a composition gap that users hit frequently.
- **Two-dot git ranges in filter selectors** (v2.9.7-canary.1, [PR #12599](https://github.com/vercel/turborepo/pull/12599)).
- **Vercel auth moved to standard OAuth/device flows** (v2.9.7-canary.11, [PR #12526](https://github.com/vercel/turborepo/pull/12526)). Replaces proprietary auth with OAuth; canary.12 preserves legacy Vercel auth compat. Signal: Vercel is modernizing `turbo login` to match industry auth conventions.
- **pnpm v11 multi-document lockfile support** (v2.9.7-canary.7, [PR #12616](https://github.com/vercel/turborepo/pull/12616)). Track for Nx pnpm lockfile parser parity.
- **PTY graceful-shutdown fixes across canary.8-10** -- exit code preservation, Node wrapper kept alive during shutdown, PTY graceful semantics. Suggests Turbo's PTY path had reliability issues under signals/CI cancellations.

#### Performance

- **Remote cache upload chunk size raised 8KB -> 256KB** (v2.9.5, [PR #12568](https://github.com/vercel/turborepo/pull/12568)).
- **`boundaries` checking parallelized with Rayon, DFS traversals cached** (v2.9.5, [PR #12569](https://github.com/vercel/turborepo/pull/12569)).

#### DX

- **Actionable error when remote cache requested but not linked** (v2.9.6, [PR #12584](https://github.com/vercel/turborepo/pull/12584)).
- **Concurrency error references `turbo.json`** directly (v2.9.6, [PR #12582](https://github.com/vercel/turborepo/pull/12582)).
- **Custom CA cert loading** in fast webpki HTTP client (v2.9.6, [PR #12591](https://github.com/vercel/turborepo/pull/12591)) -- enterprise proxy/TLS support.
- **Watch loop unblocked for interruptible persistent tasks** (v2.9.2, [PR #12509](https://github.com/vercel/turborepo/pull/12509)).

#### AI / Agent

- **`@vercel/agent-readability` replaces local ai-agent-detection** (v2.9.4, [PR #12528](https://github.com/vercel/turborepo/pull/12528)). Turbo is centralizing agent-aware content rewriting into a Vercel-owned library -- suggests Vercel is building a reusable agent-readability primitive across their docs/SDK surface.
- **`cursor[bot]` making direct commits** to turborepo examples (v2.9.7-canary.1). Vercel is using Cursor agents to maintain the repo. Signal, not feature.
- **Turborepo Agent Skills** referenced in the 2.9 blog -- coordinated with `turbo query` stable to pitch "agents querying your monorepo."

#### Deprecations (prep for 3.0)

This is the most strategic part of 2.9. Per the [release blog](https://turborepo.dev/blog/2-9), deprecated in 2.9 (to be removed in 3.0):

| Deprecated | Replacement |
|---|---|
| `turbo-ignore` | `turbo query affected` |
| `turbo scan` | (removed, functionality obsolete) |
| `--parallel` | `persistent` + `with` in turbo.json |
| `--no-cache` | `--cache=local:r,remote:r` |
| `TURBO_REMOTE_ONLY`, `--remote-only` | `--cache=remote:rw` |
| `TURBO_REMOTE_CACHE_READ_ONLY`, `--remote-cache-read-only` | `--cache=local:rw,remote:r` |
| `TURBO_DAEMON`, `--daemon`, `--no-daemon`, `daemon` in turbo.json | **Daemon no longer used** |
| `--graph` with png/jpg/pdf | svg/html/mermaid/dot |
| `--graph` with json | `turbo query` |
| `--scope` for `turbo prune` | positional args |

Two takeaways:

1. **Turborepo is removing its daemon entirely** in 3.0. Their startup-overhead story (96% faster TTFT) is now supposed to be good enough without one. This is the opposite of Moon's move (which just added a daemon) and re-emphasizes Nx's daemon as a differentiated bet. Worth testing whether Turbo's startup actually matches Nx daemon-mode on warm invocations.
2. **`--cache` as unified cache-mode flag** consolidates 5+ env vars/flags into one composable string. Good DX move; Nx should evaluate whether our cache controls (`--skip-nx-cache`, `NX_NO_CLOUD`, etc.) would benefit from similar consolidation.

### Nx implications

- **`--affected` + `--filter` composition gap closed** -- users asking for this on our side now have a Turbo answer. We should confirm our own behavior matches or exceeds.
- **`turbo query` stable** is now a marketable agent surface. Our MCP tools (`nx-mcp`) cover the same ground but we should ensure parity messaging: we are already shipping MCP-native, not GraphQL. That's arguably a better agent surface (agents don't need to know GraphQL). Consider a positioning post: "Why Nx chose MCP over GraphQL for agent queries."
- **Daemon removal in 3.0** reframes Nx's daemon as either (a) a strategic advantage (better warm-path performance) or (b) legacy weight if Turbo's TTFT claims hold. We should benchmark Nx cold/warm vs Turbo 2.9 cold in realistic monorepos before Turbo 3.0 lands.
- **Circular package dependency tolerance** removes a pain point in adoption from Turbo-land. Worth checking whether `nx import` and `nx init` on cyclic workspaces behaves gracefully.
- **`cacheMaxAge` / `cacheMaxSize`** -- do we have declarative equivalents? If not, consider adding to `nx.json` caching config.

## Moon

**Latest stable**: v2.2.2 (2026-04-20) -- npm `@moonrepo/cli@latest` = 2.2.2 (verified 2026-04-20)
**Releases this period**: v2.0.0 (Feb 18), v2.0.1, v2.0.3, v2.0.4, v2.1.0 (Mar 16), v2.1.1, v2.1.2, v2.1.3, v2.1.4 (Apr 4), **v2.2.0** (Apr 12), **v2.2.1** (Apr 16), **v2.2.2** (Apr 20). 12 stable releases in 60 days.
**Blog**: [moon v2.2 -- Daemon, AI skills, async graph building, async affected tracking, and more](https://moonrepo.dev/blog/moon-v2.2) (Apr 13)

### New since 2026-04-16 scan (v2.2.1 + v2.2.2)

**v2.2.1 (Apr 16)** -- follow-up patch with minor capability improvements:

- **Async affected tracking another +5-10%** on top of the v2.2.0 146% gain. Continued perf hammer on the v2.2 headline feature.
- **pnpm v10 multi-document lockfile parsing** fix (for `managePackageManagerVersions` enabled, which is default). **Parallel to Turbo's v2.9.7-canary.7 pnpm v11 multi-doc lockfile fix** -- both tools racing to match pnpm's evolving lockfile formats.
- **Remote cache errors now treated as cache misses** -- previously errored on pre-execution cache check. Resilience improvement; signals the remote cache path was fragile at v2.2.0.
- **Request retries via proto** (3 retries with exponential backoff for transient network/rate-limit errors) -- cloud-client hardening.
- **TypeScript project references prefixed with `./`** in prep for TypeScript v7 (`baseUrl` removal). Staying current with TS roadmap.
- Fixed `moon ci --summary`, PowerShell `-EncodedCommand` switch, graph visualizer JS errors post-v2.2 graph data-shape change.

**v2.2.2 (Apr 20)** -- regression patch:

- **Plugin loading perf regression fix** -- v2.2.0's graph changes slowed plugin loading. **Signal**: the v2.2.0 async graph builder had real side-effects Moon is still unwinding.
- **VS Code extension updated to v2 JSON schemas**. Users on moon v1 must pin an older extension version. First concrete sign the v1 -> v2 migration story is still painful.
- Python toolchain pip/uv dependency direction fix.
- `unpack` extension missing-plugin-locator fix.

### Key changes (v2.2.0 -- headline release)

#### Features

- **Daemon (unstable)** in v2.2.0. `moon daemon start/stop`, file watcher, cache invalidation. Setting: `unstable_daemon` in `.moon/workspace.*`. [v2.2.0 notes](https://github.com/moonrepo/moon/releases/tag/v2.2.0). **Headline feature and a direct structural catch-up to Nx.** Auto-starts on first use; CLI falls back to in-process graph if unavailable.
- **AI Skills for agents (NEW this scan)** -- v2.2 ships a `debug-task` skill targeting Claude and compatible AI agents. 5-step diagnostic workflow: inspect resolved task config after inheritance merging, run with max verbosity, inspect cache state and hash manifests, match symptoms to root causes via built-in decision tree, validate the fix. Skills install via CLI and invoke by describing task problems to the AI. **This is Moon's first explicit agent-facing product surface** and a direct counterpart to Nx's MCP self-healing flow. [Blog](https://moonrepo.dev/blog/moon-v2.2).
- **Async affected tracker** -- 146% speedup at 15k tasks (revised from prior "100-150%"). Gated behind `experiments.asyncAffectedTracking` (v2.2.0). Project-only tracking shows minimal improvement; gains concentrate in task-heavy scenarios.
- **Async project/task graph builder** -- 170% speedup at 5k projects / 20k tasks. Gated behind `experiments.asyncGraphBuilding` (v2.2.0). Does not support cycles in async mode. Known limitation: does not cache results yet -- rebuilds from scratch each invocation.
- **On-demand affected marking** (v2.1.2) -- tracker marks projects/tasks as affected lazily to short-circuit early.
- **`affectedFiles` settings**: `filter`, `ignoreProjectBoundary`, `passDotWhenNoResults` (v2.1.0).
- **`runInSyncPhase`** task option (v2.1.0) -- run tasks during `moon sync`.
- **Environment detection now recognizes remote devboxes** (Codespaces, Gitpod) -- v2.1.0.
- **TypeScript v6 support** (v2.1.2).
- **`pruneProjectReferences`** setting (v2.1.0).
- **System toolchain now built-in** (not WASM) -- v2.2.0. Boot-time improvement.

#### Performance

- Graph memory footprint reduction (nodes as integers, v2.2.0).
- Plugin registry faster loading (v2.2.0).
- Task output archiving off main thread pool (v2.2.0).

#### Breaking

- **`--json` output format changed** for `moon action-graph`, `moon project-graph`, `moon task-graph` in v2.2.0. Nodes are now integers + separate `data` map. Consumers must update.

### Nx implications

- **Moon finally has a daemon.** This converges Moon's architecture with Nx's core differentiator. On our side, we should emphasize daemon maturity: our daemon is stable, theirs is unstable-flagged. But the gap is closing.
- **Moon's async graph building is directly competitive with Nx's Rust-based project graph.** We're both in the "background thread pool per project" camp. We should verify our project graph build times stay ahead in the 500+ project range.
- **Minor opportunity**: Moon's removal of WASM for the system toolchain is an admission that WASM-plugin overhead hurts. Nx uses native modules and is well-positioned here.
- **Moon is investing heavily in polyglot toolchain inference** (Go `go list --deps`, Python extras metadata, Rust Cargo, TS v6) -- same space as Nx's `@nx/gradle`, `@nx/dotnet`, etc.
- **Moon AI Skills (`debug-task`) is a direct parallel to Nx MCP's self-healing flow.** Moon's is limited to task debugging; Nx MCP covers broader agent operations. We should position this as "Moon is catching up to what Nx MCP already does" in content, and accelerate MCP skills breadth before Moon expands their skills catalog. **Action: review Moon's skill packaging/distribution model -- if they ship skills as installable packages, Nx may want a similar distribution surface for MCP tools.**

## Bazel

**Latest stable**: 9.0.2 (2026-04-09)
**Upcoming**: 9.1.0rc3 (2026-04-15) -- advancing through RCs, 3 released in 6 days
**Previous stable in period**: 8.6.0 (Feb 26), 9.0.1 (Mar 10), 9.0.2 (Apr 9)

### Key changes

#### 9.0.2 (patch) - [release notes](https://github.com/bazelbuild/bazel/releases/tag/9.0.2)

- Local and remote repo contents cache now include host OS + CPU architecture in cache key ([#29194](https://github.com/bazelbuild/bazel/pull/29194)). Correctness fix.
- Include verification aware of Windows case-insensitivity ([#29214](https://github.com/bazelbuild/bazel/pull/29214)).
- `RUNFILES_TREE_MARKER` treated as directory in `BuildEvent.forArtifact()` ([#29010](https://github.com/bazelbuild/bazel/pull/29010)).
- Platform-specific startup bazelrc flags ([#29251](https://github.com/bazelbuild/bazel/pull/29251)).

#### 9.1.0rc1 / rc2 / rc3 (new minor) - [release notes rc3](https://github.com/bazelbuild/bazel/releases/tag/9.1.0rc3)

- **`--experimental_remote_cache_chunking`** -- read/write large blobs to remote cache in chunks ([#28903](https://github.com/bazelbuild/bazel/pull/28903)). Requires server support. Large-artifact remote cache performance.
- **`--rewind_lost_inputs` (experimental)** -- rerun actions within a single build to recover from cache eviction ([#28958](https://github.com/bazelbuild/bazel/pull/28958)). Enterprise resilience feature. If you're on a hot-eviction cache, Bazel can now recover mid-build.
- **Persistent worker protocol forwarding** (`persistentWorkerProtocol` platform property) for intermixing JSON/Proto workers across a build ([#28710](https://github.com/bazelbuild/bazel/pull/28710)).
- `compatibility_level` and `max_compatibility_level` attributes in MODULE.bazel are now no-ops ([#28616](https://github.com/bazelbuild/bazel/pull/28616)).
- Remote repo contents cache supports all reproducible repo rules ([#29225](https://github.com/bazelbuild/bazel/pull/29225)).
- **rc2 (Apr 14) -- CcInfo compatibility notice** ([release notes](https://github.com/bazelbuild/bazel/releases/tag/9.1.0rc2)). Bazel 9 removed `CcInfo` symbol (intentional, Starlarkification), but 9.0.0 failed silently; 9.0.1 [#28360](https://github.com/bazelbuild/bazel/pull/28360) surfaces the error correctly. Users must upgrade `rules_go` >= 0.59.0 and `rules_nodejs` >= 6.7.3. Significant migration friction for Bazel-on-JS users. **Validates Nx's decision not to compete in the Bazel-plus-rules_nodejs space.**
- **rc3 (Apr 15)** adds cross-build launcher fix (build Windows binaries on Linux, [#29239](https://github.com/bazelbuild/bazel/pull/29239)), `rctx.symlink` implicit watch on copy-fallback ([#28741](https://github.com/bazelbuild/bazel/pull/28741)), `package_group` supports external repo labels ([#28893](https://github.com/bazelbuild/bazel/pull/28893)).
- **Incompatible**: `string.splitlines()` no longer treats u+0085 (NEL) as newline ([#28931](https://github.com/bazelbuild/bazel/pull/28931)).

### Nx implications

- **`--rewind_lost_inputs` is interesting**. Nx Cloud cache eviction doesn't currently auto-retry mid-run. Worth a product conversation about whether we surface a similar capability.
- **Remote cache chunking** -- relevant to Nx Cloud's remote cache artifact sizing. Track whether customers with large artifacts (docker layers, large bundle outputs) are hitting transfer limits.
- Bazel remains the enterprise-resilience benchmark. Nothing here that threatens Nx's JS/TS/polyglot positioning.

## Gradle

**Latest stable**: 9.4.1 (2026-03-19, [release page](https://gradle.org/releases/))
**Latest RC**: **9.5.0-RC3 (2026-04-17)** -- new since last scan ([release notes](https://docs.gradle.org/9.5.0-rc-3/release-notes.html))
**9.5 RC train**: RC1 (Mar 30), RC2 (Apr 9), **RC3 (Apr 17)** -- cadence has re-accelerated after appearing stalled at the Apr 16 scan
**New blog posts this period**:
- [Avoiding the Next Supply Chain Disaster with GitHub and Gradle](https://blog.gradle.org/) (Feb 25)
- [GitHub Actions for Gradle v6: What's Changing and Why](https://blog.gradle.org/) (Mar 24)
- [Choice, Clarity, and the Future of Caching in Gradle Actions](https://blog.gradle.org/) (Apr 3)

### Gradle 9.5.0-RC3 headline features (NEW this scan)

Verified directly from the [9.5.0-RC3 release notes](https://docs.gradle.org/9.5.0-rc-3/release-notes.html):

- **Task provenance in reports and failure messages.** Task failures now surface which script (build script / settings script / plugin) and which source registered the failing task. **Debuggability feature that encroaches on Nx's Project Details / cache debugging framing** -- users get "why is this task failing and where did it come from" natively in the build output.
- **Precompiled Kotlin Settings plugins** -- type-safe accessors auto-generated for Settings convention plugins. IDE autocompletion + compile-time validation at the settings level (previously only project-level).
- **Wrapper download retries** -- `gradle-wrapper.properties` accepts retry attempts and backoff config. Network resilience for CI.
- **`--develocity-url` CLI flag** -- publish Build Scan without modifying project config. **Signal**: Develocity is being lowered from project-config citizen to CLI-override citizen. Makes Develocity adoption frictionless for agents/CI scripts.
- **`disallowChanges()` on domain object collections** -- plugin authors can lock collections post-configuration. Plugin-correctness feature.
- **No AI/agent features in 9.5.** Confirmed from RC3 release notes. Gradle continues to concentrate AI surface area in **Develocity** rather than core.

RC1 and RC2 previously flagged "Task provenance", "Precompiled Kotlin Settings plugins", "Build authoring improvements." RC3 adds Wrapper retries, `--develocity-url`, `disallowChanges()`.

### Key changes (non-core)

No new Gradle core stable release since 9.4.1.

**April 3 blog post "Choice, Clarity, and the Future of Caching in Gradle Actions"** by Daz DeBoer ([link](https://blog.gradle.org/choice-clarity-future-caching-gradle-actions)) -- read in full this scan. Gradle Actions v6 had introduced proprietary "Enhanced Caching" as default, drawing community pushback about data ownership and OSS requirements. **v6.1.0 response**: (1) "Safe Harbor Clause" -- `"Your code and artifacts belong to you. Period."` explicit metadata/usage-metric-only claims; (2) restored fully open-source `basic` caching via `cache-provider: basic` -- MIT-licensed wrapper around GitHub's native `actions/cache`. Maintainers publicly acknowledged they "missed the mark" on communication. **Both caching modes free for public repos; Enhanced Caching free preview for private repos with future limits for large commercial users.**

### Nx implications

- **Gradle 9.5 is approaching**: `@nx/gradle` should validate against 9.5.0-RC3 this cycle. Task provenance in reports may surface via our Gradle plugin's output parsing -- verify no regressions in how we read failure messages.
- **Task provenance is a lightweight version of what Nx Cloud does** (we show full task input diffs / cache miss analysis). Gradle's version is native to the build output; Nx Cloud's is richer but requires the cloud surface. Worth articulating as "native debuggability vs platform debuggability" in Gradle comparative content.
- **`--develocity-url` CLI lowering friction for Develocity adoption** -- this is Gradle streamlining their paid-tier onboarding. Nx Cloud equivalent already exists (`nx-cloud connect`, `NX_CLOUD_ACCESS_TOKEN`) but consider whether our CLI-override story is equivalently clean.
- Supply-chain post aligns with Nx's own NXC-4197 expand-deps supply chain hardening work -- validates the theme.
- **Gradle Actions caching fiasco is instructive for Nx Cloud positioning**. Key learnings: (1) ownership language matters -- "your code belongs to you" should be explicit in Nx Cloud marketing; (2) a free OSS-compatible caching alternative beside the paid product reduces friction; (3) community trust can be broken by default-enabled proprietary tooling. Nx Cloud's current positioning (optional, opt-in) is already aligned but the Gradle situation is a good cautionary tale when we iterate on Cloud defaults.

## Pants

**Latest stable**: 2.31.0 (Feb 20) -- unchanged
**Patch stable**: **2.30.2 (Apr 15)** -- new in this scan
**RC channel**: **2.31.1rc0 (Apr 15)** -- new patch RC for 2.31 line
**Alpha channel**: **2.32.0a0 (Apr 16)** -- first alpha release for 2.32, formalized via new GitHub Actions workflow
**Dev channel**: 2.32.0.dev7 (Apr 1) -- unchanged since prior scan
**Releases in period**: dev2, dev3, dev4, dev5, dev6, dev7, 2.30.2, 2.31.1rc0, 2.32.0a0

### Key changes

#### Release process (NEW this scan)

- **Formal alpha-release channel introduced** ([#23259](https://github.com/pantsbuild/pants/pull/23259), [#23262](https://github.com/pantsbuild/pants/pull/23262)). Pants added a dedicated "Alpha PR creation" workflow and shipped `2.32.0a0` (Apr 16) -- their first versioned alpha. Previously they had dev -> rc -> stable; now dev -> alpha -> rc -> stable. **Signal**: Pants is stabilizing its release train management, likely in preparation for pants-ng rollout.
- **Patch stable shipped** (2.30.2, Apr 15) -- cherry-picked Trivy v0.69.2 upgrade. Ongoing LTS maintenance for 2.30 line.
- **2.31.1rc0** (Apr 15) -- Trivy cherry-pick plus sandboxer daemon fix for inherited parent stdin/stdout file descriptors ([#23246](https://github.com/pantsbuild/pants/pull/23246)). Sandboxer is the Pants equivalent of a build daemon process.

#### Features / Infra

- **Experimental uv-based PEX builder** (dev7, [#23197](https://github.com/pantsbuild/pants/pull/23197)); **uv PEX builder uses pex3 lock export** (2.32.0a0, [#23227](https://github.com/pantsbuild/pants/pull/23227)); **UV upgraded to latest** (2.32.0a0, [#23256](https://github.com/pantsbuild/pants/pull/23256)). Pants continues heavy uv investment.
- **Pex default bumped to v2.92.2** (2.32.0a0, [#23260](https://github.com/pantsbuild/pants/pull/23260)).
- **Support ignoring certain string imports in Python dep inference** (2.32.0a0, [#23245](https://github.com/pantsbuild/pants/pull/23245)). DX improvement for mis-inferred string-literal imports.
- **Plucking option values from inside structured files** (dev7, [#23201](https://github.com/pantsbuild/pants/pull/23201)).
- **`.toml` files in option "fromfile" support** (dev7, [#23196](https://github.com/pantsbuild/pants/pull/23196)).
- **Port `frozendict` and scalar fields to Rust** (dev7 [#22501](https://github.com/pantsbuild/pants/pull/22501), 2.32.0a0 [#23204](https://github.com/pantsbuild/pants/pull/23204)). Perf + pants-ng prep.
- **pants-ng infrastructure** continues: NG `goal` concept (dev6, [#23187](https://github.com/pantsbuild/pants/pull/23187)), external binary downloads (dev6, [#23190](https://github.com/pantsbuild/pants/pull/23190)), NG subsystem (dev6, [#23165](https://github.com/pantsbuild/pants/pull/23165)). pants-ng is the Rust-native rewrite; still pre-release.
- **Rust v1.94.1 upgrade** (2.32.0a0, [#23229](https://github.com/pantsbuild/pants/pull/23229)). **pyo3 v0.28.3** (2.32.0a0, [#23230](https://github.com/pantsbuild/pants/pull/23230)).
- **Fix: preserve workunit log level in local cache metadata update** (2.32.0a0, [#23251](https://github.com/pantsbuild/pants/pull/23251)).
- **Python 3.14 devcontainer** (2.32.0a0, [#23241](https://github.com/pantsbuild/pants/pull/23241)).

#### DX / Meta

- **AI tooling notice for contributions** (dev6, [#22982](https://github.com/pantsbuild/pants/pull/22982)). Governance signal -- Pants is formally acknowledging AI-assisted contributions.
- **Preliminary discussion emphasis in contribution docs** (2.32.0a0, [#23247](https://github.com/pantsbuild/pants/pull/23247)). Governance cadence continues.
- **Zizmor workflow scanning + separated cargo-audit** (2.32.0a0, [#23249](https://github.com/pantsbuild/pants/pull/23249)). Supply-chain hygiene improvement.
- Shell completions allow filesystem completions (dev6).
- More detailed debug logging for remote cache writes (dev6).

### Nx implications

- Pants remains Python/JVM-focused; no direct JS/TS overlap. No actionable threat.
- **pants-ng Rust rewrite** is the long-term competitive watch -- when it ships stable it will close the Rust-performance gap. Still pre-release; the new alpha channel may signal pants-ng approaches its first tagged alpha in the 2.32 or 2.33 line.
- **Pants supply-chain hardening (Zizmor, cargo-audit separation)** parallels our NXC-4197 work. Another data point that supply-chain is now table stakes for monorepo tools.

## Cross-Competitor Themes

### 1. Agent-aware product surfaces (4/5 competitors, now with 2 shipping explicit skills)

- Turborepo: `@vercel/agent-readability` library, cursor[bot] committing, Agent Skills with `turbo query`.
- **Moon (NEW this scan)**: explicit **AI Skills** system shipped in v2.2 (blog Apr 13) -- `debug-task` skill for Claude and compatible AI agents; 5-step diagnostic workflow. This is a full product feature, not a governance signal.
- Pants: AI tooling notice for contributions (governance, not product yet).
- Bazel: no explicit agent move.
- Gradle: no explicit agent move.

**Implication (upgraded)**: Agent-first product design is no longer emerging -- it's **the competitive baseline for monorepo tools as of April 2026**. Two of three direct competitors (Turbo, Moon) now ship explicit agent-facing surfaces. Nx's MCP-first position (nx-mcp + self-healing CI) remains strongest, but the "we're the only one with agent integration" messaging window has closed. **We must accelerate MCP skill breadth and publish comparative content ("why MCP beats GraphQL / why Nx Agents beat debug-skills") within 30 days.**

### 2. CI-as-first-class configuration (Turbo + Moon)

- Turbo: `experimentalCI` key in turbo.json (v2.8.22 canary -> shipped in 2.9.0).
- Moon: `MOON_PIPELINE_*` env var overrides (v2.2.0).

Both adding CI-specific task/behavior differentiation. Nx Cloud already differentiates CI via DTE/NxAgents -- but the config-level CI surface is a new convention we should evaluate.

### 3. Daemon convergence (Moon arrives, Turbo leaves)

- Moon: adds unstable daemon in 2.2.0.
- Turbo: deprecating daemon entirely in 3.0.
- Nx: daemon is stable and core.

Genuine split in the industry. Whether daemons win depends on whether Turbo's "cold-start fast enough" claim survives real-world testing. Worth a benchmark post.

### 4. Async graph building (industry converging)

- Moon: `asyncGraphBuilding` experiment (v2.2.0).
- Turbo: existing Rust-native graph.
- Nx: Rust-native project graph shipped.

Converging industry bet. Nx is on the right side of this.

### 5. Cache eviction controls

- Turbo: `cacheMaxAge` / `cacheMaxSize` (v2.9.1).
- Bazel: `--rewind_lost_inputs` recovers from eviction (9.1.0rc1).
- Nx: `nx reset` + manual controls.

Nx should evaluate declarative eviction policy in `nx.json`.

## Competitive Gap Analysis

| Nx Feature | Competitor Status | Gap Direction | Urgency |
|---|---|---|---|
| Daemon | Moon v2.2.0 unstable, Turbo removing in 3.0 | Ahead (stable, mature) | Low -- defend with benchmarks |
| Project graph (Rust) | Moon async v2.2.0 (experimental), Turbo has Rust | Contested | Low |
| Affected analysis | Turbo `turbo query affected` stable + `--affected`/`--filter` compose | Contested | Medium -- verify parity |
| Cache eviction policy (declarative) | Turbo `cacheMaxAge`/`cacheMaxSize` (v2.9.1) | Behind | Medium |
| MCP / agent surface | Turbo GraphQL via `turbo query`, Moon has MCP tools | Ahead (MCP-first) | Low -- needs content |
| Remote cache chunking (large artifacts) | Bazel 9.1.0rc1 | Behind | Low -- niche |
| Mid-build cache eviction recovery | Bazel `--rewind_lost_inputs` | Behind | Low -- niche |
| CI-specific task config | Turbo `experimentalCI`, Moon `MOON_PIPELINE_*` | Behind at config level | Medium |
| Circular package-dependency adoption | Turbo v2.9.0 tolerates cycles | Behind / Same | Medium -- verify nx import |
| Startup performance (TTFT) | Turbo claims 80-96% improvement | Contested -- needs benchmark | **High** |

### Where Nx Is Behind (new this month)

- **Declarative cache eviction policy**: Turbo v2.9.1 added `cacheMaxAge`/`cacheMaxSize`. Nx relies on manual `nx reset`. Low effort to close.
- **`--affected` + `--filter` composition**: Turbo shipped in v2.9.4. Verify Nx parity; if missing, add.
- **Cold-start TTFT claims**: Turbo's 80-96% number is a marketing asset. Nx needs a counter-benchmark in warm (daemon) and cold modes. **Most urgent action.**
- **CI-specific task config key**: Turbo `experimentalCI` is a new surface convention.

### Where Nx Is Ahead

- **MCP-native agent surface**: Turbo is pitching GraphQL; Nx ships MCP. MCP is the emerging standard. Defensible but needs louder positioning.
- **Daemon maturity**: Moon unstable, Turbo removing. Nx stable.
- **Nx Cloud remote execution + DTE**: no competitor ships comparable agent distribution. Bazel remote execution exists but is infra-heavy.
- **Polyglot plugin breadth (JS/TS-first + Gradle/Rust/Python via typed plugins)**: broader than Moon or Turbo.

### Unassailable Moats

- **Nx Cloud DTE / NxAgents** -- remote task distribution with SaaS ops. Not viable for Turbo (no Vercel equivalent), Moon (no cloud), or OSS Bazel BuildBuddy peers (different model).
- **Plugin ecosystem** -- `@nx/*` family integrations with Angular, Next.js, Remix, Vite, Jest, Playwright, etc. Turbo has examples-based patterns, not typed plugins.
- **Nx Agents self-healing / MCP self-healing flow** -- not matched by any competitor.

## Recommended Actions

1. **Benchmark Nx cold + warm TTFT vs Turborepo 2.9.x on realistic monorepos (100, 500, 1000 projects)** and publish. Turbo's "80-96% faster" is marketing; we need a defensible counter-number before Turbo 3.0 lands. **Highest-urgency competitive response this month.**
2. **Write "Why Nx chose MCP over GraphQL for agent queries"** positioning post to counter Turbo 2.9 agent narrative. **Upgrade scope (new this scan)**: also address Moon's `debug-task` skill -- argue that Nx MCP provides broader agent operations out-of-the-box vs single-purpose skills. Lean on self-healing / MCP standard / no-GraphQL-required angles.
3. **Review Moon AI Skills packaging/distribution** (new) -- if they ship skills as installable packages, evaluate whether Nx should provide a similar installable-skill surface (rather than everything bundled into nx-mcp). Publish a skills/playbook catalog if viable.
4. **Add declarative cache eviction policy to `nx.json`** (e.g. `cache.maxAge`, `cache.maxSizeBytes`). Small effort, closes a Turbo feature gap.
5. **Verify `--affected` + `--filter` composition in Nx CLI matches or exceeds Turbo v2.9.4.** If gaps exist, close them.
6. **Audit `nx init` / `nx import` behavior on workspaces with cyclic package deps.** Turbo now tolerates these; Nx should not regress relative.
7. **Benchmark Nx daemon stability claims vs Moon v2.2.0 unstable daemon** in a blog post. Moon's arrival validates Nx's bet; capitalize on the timing.
8. **Gradle Actions caching lessons** -- review Nx Cloud opt-in defaults and user-data language through the Gradle v6 -> v6.1 lens. Consider adding an explicit "Your code and artifacts belong to you" clause to Nx Cloud marketing/TOS.
9. **Track Turbo 3.0 daemon-removal impact** through Q3. If real users file performance issues post-daemon-removal, that's content gold for Nx daemon advocacy.
10. **Monitor Pants alpha channel** (new) -- the 2.32.0a0 alpha formalization suggests pants-ng may reach tagged alpha within 1-2 Pants minor cycles. Longer horizon, but worth flagging when Pants-ng enters alpha.
11. **Validate `@nx/gradle` against Gradle 9.5.0-RC3** (new this scan) -- task provenance in reports may change the shape of failure output we parse. Run plugin integration tests before 9.5 GA ships.
12. **Counter-message Gradle's "task provenance"** (new) -- Gradle's native "which script registered this failing task" surface is a lightweight Nx Cloud lookalike. Nx Cloud's task-input diffing and cache miss analysis are richer. Draft a comparison blurb for the `@nx/gradle` docs that positions the two tiers of debuggability.
13. **Track Turbo v2.9.7 stable cut** -- the OAuth-heavy canary train signals an imminent stable release. Expected to include standard OAuth/device-flow auth (potentially a marketing moment for Vercel tying `turbo login` to their broader platform identity story).
