# Competitor Intel -- June 2026

_Last updated: 2026-06-22_
_Period: May 26 - June 22, 2026 (delta since 2026-05 W22 scan; W3 update June 8-22)_
_Lookback window: 60 days back to 2026-04-23 for full context_

## Executive Summary

- **Moon v2.3.0 is the cycle's strategic headline.** Shipped June 1 with CAS cache experiment, native file hashing (10-50% perf gain), MCP template-discovery tools, task tags, and 50-100% task-target memory reduction. Moon threat remains HIGH.
- **BOTH Turborepo (v2.9.18, June 10) and Moon (v2.3.3, June 10) patched cache-related security vulnerabilities on the same day.** Turbo: 7 security/hardening fixes including git argument injection prevention and cache output containment. Moon: remote cache manifest poisoning allows arbitrary file writes outside output list. Cache security is now a cross-competitor concern.
- **Turborepo removed its web UI mode in v2.9.18** -- a product scope contraction, likely security-motivated (query server file access was also hardened in same release). Still no v2.9.19 stable or 3.0 RC (14 weeks since March narrative).
- **Gradle 9.6.0 FINAL shipped June 20** with the config cache hit rate improvements previewed in RC1. Now marketable: project property change tracking in CI significantly improves cache hit rates.
- **Bazel 9.1.1 (June 3) remains the latest.** No new releases since.
- **Pants 2.32.0 final shipped May 29** as predicted. 2.33.0.dev3 (June 12) moves PathGlobs and Filespec to Rust for performance. No JS/TS crossover.

---

## Turborepo

**Latest stable:** v2.9.16 (May 28, 2026)
**Latest canary:** v2.9.17-canary.5 (June 7, 2026)
**Releases since May-25 scan:** v2.9.15 (May 26) -> v2.9.16 (May 28) -> v2.9.17-canary.{1-5} (May 28-June 7)

### Released this period

**v2.9.15 (May 26) -- Stability + experimentalCI**

This was the graduation of the canary.5-7 hardening sprint flagged in the May report:
- Extensive Rust `panic!/expect/unwrap` removal across all core modules (11+ PRs)
- Cache restore symlink race fixes (3 separate hardening passes)
- Daemon discovery response validation, process tree completion timing, Windows drain hang fix
- Non-UTF-8 Git path preservation, UTF-8 boundary panic prevention
- `experimentalCI` object config accepted (still undocumented)
- Heap allocation profiling capability
- macOS release binary signing; pnpm minimum release age set
- Lockfile-changed packages included in affected task detection
- OTEL endpoint validation strengthened

**v2.9.16 (May 28) -- Bug fixes**

- PTY shutdown hang resolved
- npm tlog publish failures now retried
- Nested Bun dependency versions preserved
- `--no-git` flag no longer deletes existing `.git` directories

**v2.9.17-canary.1 through canary.5 (May 28 - June 7)**

- Auth HTTP timeouts implemented (canary.2) -- hardens the OAuth flow from v2.9.7
- Affected root task detection improved (canary.2)
- Windows graceful shutdown fixed (canary.2)
- Rsbuild examples added (canary.3) -- new framework support
- Peer dependency handling in package graphs fixed (canary.4)
- pnpm override-resolved prune dependencies preserved (canary.5)
- PTY stdin left open for tasks fixed (canary.5)
- **P-521 ECDSA certificate chain support** added to api-client (canary.5) -- enterprise crypto compatibility
- aarch64 musl release builds restored (canary.5)

**v2.9.18 (June 10) -- MAJOR SECURITY RELEASE: 7 hardening fixes + web UI removed**

This is the second concentrated security push in 4 weeks (v2.9.14 was May 14):

- **chore: Remove web UI mode** (#13052) -- deliberate removal of browser-based task output viewer; almost certainly security-motivated given the simultaneous query server hardening
- **fix: Harden cache archive symlink restore** (#13051) -- tightens symlink handling during cache extraction
- **fix: Harden query server file access** (#13053) -- restricted file access on the local query server (previously backed the web UI)
- **fix: Confine prune patch paths** (#13054) -- limits scope of prune patch file paths to prevent escape
- **fix: Prevent git argument injection in SCM refs** (#13055) -- blocks injection into git CLI calls via repo/branch names; HIGH severity class
- **fix: Strip special mode bits from cache restore** (#13056) -- removes setuid/setgid bits from cached files; privilege escalation hardening
- **fix: Contain incremental cache outputs** (#13057) -- scopes incremental cache output files to prevent path traversal
- **fix: Block self-hosted login URLs from attempting to use Vercel's SSO** (#13061) -- prevents self-hosted Turbo instances from being used to phish Vercel credentials
- Normalize Windows daemon path hash, preserve vt100 cell byte counts, separate artifact signature fields, validate OidHash hex buffers

**v2.9.19-canary.6 through canary.9 (June 16-17)**

- Prevent duplicate PTY graceful shutdown signals (canary.7)
- Cache outputs through internal symlinks (canary.6) -- functional fix, not just hardening
- Ignore symlink cycles when caching outputs (canary.9) -- prevents cache hangs on circular symlinks
- Filtered pruned pnpm workspace patches (canary.6)
- bun `patchedDependencies` preserved during prune (canary.9)
- Precomputed unblocked JIT descendants (canary.6) -- engine performance
- Optimized default create-turbo download speed (canary.6)
- `allow_no_package_manager` threaded to daemon and watcher (canary.9)
- Graceful nested PTY process stopping (canary.8)

### Key changes by category

- **Performance:** No significant perf work this period; prior cycle's gitignore indexing and OTEL trimming shipped in v2.9.15. JIT descendant precompute in canary.6 is minor.
- **DX:** Rsbuild example (v2.9.17), better symlink cycle handling in caching (canary).
- **Security/Reliability (v2.9.18):** Git argument injection prevention, cache archive hardening, query server lockdown, privilege bit stripping, incremental cache containment, SSO phishing block. Web UI mode removed. Second major security batch in 4 weeks.
- **Enterprise:** P-521 ECDSA (v2.9.17-canary.5). Self-hosted SSO isolation (v2.9.18) is enterprise-relevant.
- **AI:** None. The `experimentalCI` object remains the only forward-looking config hint.
- **Ecosystem:** Rsbuild examples confirm Turbo is explicitly supporting Rspack/Rsbuild users.

### Nx implications

**Threat level: HIGH (unchanged).**

1. **No 2.9.19 stable or 3.0 RC** -- The March 25 "preparing for 3.0" blog is now 14 weeks old with no stable advance. Window for Nx positioning remains open.
2. **Web UI removal** is a scope contraction. Turbo had a browser-based task viewer; it's gone now. Nx Console remains the richer IDE-integrated experience. Net positive for Nx positioning.
3. **Two security batches in 4 weeks (v2.9.14 + v2.9.18)** = Turbo is under active external security research. Their 14-day response time is good, but the volume of findings is notable. Nx should maintain its equivalent incident response posture.
4. **Git argument injection fix (#13055)** affects any tool that passes user-controlled strings to git CLI. This is a class of bug worth auditing in Nx's own git operations (e.g., `nx affected --base <branch>`, remote-cache tag metadata).
5. **`experimentalCI` object config** continues to ship in canaries without docs. When it surfaces, assess against DTE/Nx Cloud CI story.
6. **Rsbuild examples** confirm Turbo is targeting Rspack/Rsbuild users. Nx has `@nx/rspack` -- check parity holds.
7. **Self-hosted SSO isolation** (#13061) suggests Turbo has self-hosted enterprise deployments. Watch for a self-hosted Turbo Remote Cache product announcement.

Sources:
- [Turborepo releases](https://github.com/vercel/turborepo/releases)
- [Turborepo npm](https://www.npmjs.com/package/turbo)

---

## Moon

**Latest stable:** v2.3.2 (June 5, 2026)
**Prior stable at last scan:** v2.2.5 (May 21)
**Releases since May-25 scan:** v2.2.6 (May 28) -> v2.3.0 (June 1) -> v2.3.1 (June 4) -> v2.3.2 (June 5)

### Released this period

**v2.2.6 (May 28) -- Go toolchain**

- Go v1.24 support for `ignore` directives
- Vendor directory scanning fix during dependency installation

**v2.3.0 (June 1) -- MAJOR RELEASE: CAS cache, native hashing, MCP tools, task tags**

This is the most significant Moon release since v2.2.0 (April 12). Key features:

- **CAS Output Cache Experiment:** Task outputs stored in local content-addressable storage, matching the remote cache format. Enables deduplication across tasks and a unified local/remote cache structure. Enable via `experiments.casOutputsCache`.
- **Native File Hashing Experiment:** Custom native hashing implementation running in the task pool, targeting 10-50% performance improvement. Enable via `experiments.nativeFileHashing`.
- **MCP Template Tools:** Two new tools added to Moon's MCP server: `get_template` (inspect a template and its variable schema) and `get_templates` (list all available templates). Enables AI assistants to discover code generation templates before generating code.
- **Task Tags:** Tasks now support a `tags` setting and `#tag` syntax for target addressing (e.g., `app:#quality`). `--tags` option added to `moon query tasks`. `options.mergeTags` controls inheritance.
- **Cache Strategy for Task Dependencies:** New `cacheStrategy` field controls invalidation behavior per dependency: `hash` (default for output-declaring deps), `ignored` (default for others), or `outputs`. Reduces unnecessary cache invalidations for lint/test tasks.
- **Task Target Memory:** 50-100% reduction in memory footprint per task target.
- **SHA256 Git Support:** Prepares for Git's eventual SHA1 -> SHA256 migration.
- **Deno v2.8 Support:** `deno ci` for installs, `deno.lock` in CI, `--prod` flag, `catalog:` reference resolution.
- **Python UV Pip:** New unstable `uv-pip` package manager option.
- **Daemon Improvements:** Auto-clean and webhooks now run from daemon instead of at process end.
- **Glob regression fix:** Unbounded walks up to 10x slower -- fixed.

**v2.3.1 (June 4) -- Daemon + toolchain**

- In-memory caching for toolchain operations (locating executables) -- faster tool resolution
- Daemon startup faster: workspace graph loaded in background after server readiness
- Plugin distribution moved to ghcr.io (away from raw URLs)
- Daemon connect/ready logic fix for potential Windows connection issues
- `cacheStrategy` inference fix when experiments enabled
- Leftover locks at `.moon/cache/locks` now cleaned up

**v2.3.2 (June 5) -- Daemon hardening + Go**

- Panic handling added to daemon server -- unexpected panics now captured and logged
- Pipeline continues if daemon client cannot connect (no hard failure)
- Go: `inferRelationshipsPackages` setting for `go list --deps` package patterns
- Go: Default dependency scan now covers all packages (`./...`) including `internal/` and `pkg/`
- `project:^` input resolution bug fixed for overlapping project sources

**v2.3.3 (June 10) -- SECURITY: Remote cache path traversal**

- **Security fix: task outputs being hydrated from the remote cache can overwrite files outside the output list if the manifest in the remote cache has been compromised.** This is a path traversal-adjacent vulnerability -- an attacker with write access to the remote cache can manipulate the manifest to cause arbitrary file writes on the developer or CI machine running moon. No CVE assigned in release notes. Severity: moderate to high depending on remote cache trust model.
- Go toolchain regression: `go.mod` parsing of `tool ()` blocks (introduced in v2.3.0-v2.3.2) corrected.

**v2.3.4 (June 18) -- Toolchain maintenance**

- Docker: Fixed pruning when multiple projects share the same name
- Python: `uv sync` now uses `--no-dev` flag (prod-only installs by default)
- Rust: `cargo-binstall` uses `--locked` (lockfile-pinned binary installs)
- Remote caching updates (details not published in release notes)

### Key changes by category

- **Features:** Task tags, cacheStrategy, SHA256 git, UV pip unstable (v2.3.0)
- **Performance:** CAS cache (experiment), native file hashing 10-50% (experiment), 50-100% task memory reduction, glob fix (up to 10x), in-memory toolchain caching (v2.3.0-2.3.1)
- **DX:** Deno v2.8, UV pip, daemon UX, Python `--no-dev` default, Rust `--locked` default (v2.3.0-2.3.4)
- **Ecosystem:** ghcr.io plugin distribution, Go v1.24 + inferRelationshipsPackages (v2.3.0-2.3.2)
- **AI:** `get_template` + `get_templates` MCP tools -- AI agents can now discover and inspect Moon's template library (v2.3.0)
- **Security:** Remote cache manifest path traversal patched (v2.3.3) -- no CVE; no advisory number

### Nx implications

**Threat level: HIGH (unchanged).**

Moon jumped from six weeks of bug-fix-only to a major minor in one cycle. Several features have direct Nx relevance:

1. **CAS local cache** is strategically significant. Moon's local cache now matches its remote cache format -- enabling deduplication and a unified caching story. Nx's local cache is not CAS-based. If the Moon experiment stabilizes, Moon will be able to claim "same caching semantics locally and remotely." This is a differentiator claim Nx cannot currently make. **Recommended: investigate whether Nx's roadmap includes CAS-format local cache.**
2. **Native file hashing 10-50% improvement** is a benchmarkable claim. Nx uses Rust via the Nx daemon for file hashing too. If Moon can ship a concrete benchmark (e.g., "hashes a 50k-file repo 40% faster"), it becomes a sales point in large-workspace conversations. **Recommended: have a comparable Nx file-hashing benchmark ready.**
3. **MCP template tools** directly compete with Nx generators as an AI-discoverable workflow. Moon's AI agents can now list templates, inspect their variable schemas, and generate code via the MCP server. Nx's generators are not yet MCP-discoverable in the same structured way. **Recommended: assess whether Nx Console's or Nx MCP's generator tooling exposes a comparable `list_generators`/`describe_generator` interface.**
4. **Task tags** give Moon a grouping primitive Nx doesn't have as first-class syntax. Nx uses project tags for filtering; Moon's `#tag` syntax targets tasks specifically. Low immediate threat but worth tracking as a DX differentiator.
5. **Daemon resilience (pipeline continues on disconnect)** -- Moon is hardening its daemon story while last scan noted Turbo was removing daemon. Two competing bets: Moon going harder on daemon, Turbo going daemon-free. Nx's daemon-based approach aligns with Moon's direction, not Turbo's.

Sources:
- [Moon releases](https://github.com/moonrepo/moon/releases)
- [Moon v2.3 blog](https://moonrepo.dev/blog/moon-v2.3)
- [Moon MCP docs](https://moonrepo.dev/docs/guides/mcp)
- [Moon v2.3.3 release](https://github.com/moonrepo/moon/releases/tag/v2.3.3)

---

## Bazel

**Latest LTS:** v9.1.1 (June 3, 2026) -- patch on 9.1.0 (April 20)
**Latest 8.x LTS:** v8.7.0 (May 7) -- unchanged

### Released this period

**v9.1.1 (June 3) -- Patch**

- Reverted `--downloader_config` as a repeatable flag (regression introduced in 9.1.0)
- Fixed crashes with `file://` URIs and opaque URIs
- Remote execution: fixed build failures when input files were lost even with rewinding enabled
- Fixed JVM crash in GrpcCacheClient
- Fixed `--remote_default_exec_properties` duplicate key crashes
- Improved blob verification during chunk downloads
- Bounded parallel chunk transfer support
- Fixed symlink info invalidation after deletion in RAFS
- CcInfo symbol removal: downstream rules must upgrade (rules_go v0.59.0+, rules_nodejs v6.7.3+)

### Nx implications

**Threat level: Low (unchanged).** Patch-only cycle; all fixes are remote-exec infrastructure. No JS/TS crossover.

Sources:
- [Bazel 9.1.1 release](https://github.com/bazelbuild/bazel/releases/tag/9.1.1)

---

## Gradle

**Latest stable:** v9.6.0 (June 20, 2026) -- FINAL shipped as predicted
**Prior stable:** v9.5.1 (May 12) -- superseded
**Latest LTS:** v8.14.5 (May 7) -- unchanged

### Released this period

**v9.6.0 FINAL (June 20) -- Configuration cache + CLI**

This shipped as predicted from the RC1 (May 28) analysis. Key features now stable:

- **Configuration cache hit rate improvements (CI-targeted):** Project properties supplied via `org.gradle.project.<n>` system properties or `ORG_GRADLE_PROJECT_<n>` environment variables no longer invalidate the config cache if those properties were never read during the configuration phase. For CI builds that pass many project properties via env vars, this is a significant cache hit rate win.
- **`--non-interactive` CLI flag:** Explicitly disables interactive prompts in automated environments. Useful for Gradle in CI pipelines where stdin is not available.
- **`NO_COLOR` environment variable support:** Suppresses color output via the standard `NO_COLOR` convention (no need for Gradle-specific flags).
- **Sortable columns in HTML test reports:** Minor DX improvement for large test suites.
- **Deprecation: implicit property and method lookup in parent projects** now emits a warning. Will be removed in Gradle 10. Early adopters can enable `NO_IMPLICIT_LOOKUP_IN_PARENT_PROJECTS` feature preview.
- **Plugin author improvement:** Clearer validation errors when `@Optional` is misused on task properties.

### Nx implications

**Threat level: Low-Medium (unchanged).**

1. **Config cache now stable with the CI property-tracking fix.** Gradle's narrative is "config cache finally works in complex CI environments." The May 20 blog post (inner-loop perf) + the RC1 headline + the June 20 final = a sustained 4-week marketing wave. JVM-shop Nx Cloud sales conversations will encounter this claim. Nx needs comparable JVM-workspace cache numbers ready.
2. **The `--non-interactive` flag** is a CI-ops quality-of-life fix. Confirms Gradle is actively investing in CI usability -- same space as Nx Cloud's task orchestration story.
3. **Gradle 10 deprecations** are early signal for future break. Low Nx relevance now.

Sources:
- [Gradle 9.6.0 release notes](https://docs.gradle.org/current/release-notes.html)
- [Gradle releases](https://github.com/gradle/gradle/releases)
- [Gradle releases page](https://gradle.org/releases/)

---

## Pants

**Latest stable:** v2.32.0 (May 29, 2026) -- final shipped as predicted
**Latest dev:** v2.33.0.dev3 (June 12, 2026) -- updated
**Latest LTS patch:** v2.31.1rc1 (May 6)

### Released this period

**v2.32.0 final (May 29) -- Delivered as predicted**

- Terminal width fallback when size is 0
- Remote cache improvements for REv2.0 root-capture actions
- Missing-entry guard in CoursierResolvedLockfile

**v2.33.0.dev2 performance highlight (May 25, from prior scan boundary)**

- **22% faster `dependencies` goal** via Python coroutine/trampoline overhead removal
- OpenTelemetry backend for work unit reporting
- Docker image layer artifact extraction feature

**v2.33.0.dev3 (June 12) -- Rust performance internals**

- `SourceField`, `PathGlobs`, and `Filespec` moved from Python to Rust -- core path-matching primitives are now native
- Rust dockerfile parser improvements for build argument compatibility
- No strategic new features; this is internal performance work continuing the Rust migration of Pants' internals

### Nx implications

**Threat level: Very Low (unchanged).** Pants remains Python/Go/JVM-focused. Moving PathGlobs to Rust is the same pattern Nx uses (native Rust bindings for glob/hashing via the daemon). No direct Nx relevance, but confirms the industry shift toward Rust for build-critical path operations.

Sources:
- [Pants releases](https://github.com/pantsbuild/pants/releases)
- [Pants 2.32.0 blog](https://www.pantsbuild.org/blog/2026/05/28/pants-2-32)

---

## Cross-Competitor Themes (updated June 22)

1. **Daemon as infrastructure is bifurcating.** Moon is investing heavily in daemon hardening -- background graph loading, panic recovery, pipeline-continues-on-disconnect, webhooks from daemon. Turbo moved away from daemon. Nx's daemon-based model (Nx Daemon for project graph, local cache) aligns with Moon's bet. This could become a narrative battleground: Moon/Nx claim daemon gives richer runtime context; Turbo claims simplicity.
2. **CAS (content-addressable storage) enters the local cache space.** Moon's experiment brings CAS semantics to local task outputs. This mirrors how remote caches (Nx Cloud, Turbo Remote Cache) work. If CAS-local becomes the standard, tools without it will need to explain why. First mover in the local CAS space is Moon.
3. **MCP is becoming a build-tool battleground.** Moon now has: run_task, sync_projects, get_touched_files, get_template, get_templates. That's 5 MCP tools for AI agent integration. The addition of template-discovery tools specifically targets the AI code-generation workflow. This is the clearest indication that Moon sees AI-native workflows as a competitive axis, not just a feature.
4. **AI code generation discoverability** (templates/generators) is the new front. Moon's MCP can now answer "what can I generate?" and "what variables does this template take?" before the AI generates code. This reduces hallucination risk in AI-driven scaffolding. Nx generators are richer but not yet MCP-discoverable in this structured way.
5. **Cache security is now a cross-competitor concern.** Both Turborepo (v2.9.18, June 10) and Moon (v2.3.3, June 10) patched cache-related security vulnerabilities on the same day. Turbo fixed 7 issues including git argument injection and cache archive path escape. Moon fixed a remote cache manifest poisoning enabling arbitrary file writes. This validates that remote and local caching surfaces are under active security research. Nx's cache implementation should be audited for the same classes of bugs: manifest path traversal, symlink escape, special mode bit preservation.
6. **Enterprise crypto (P-521 ECDSA) in Turbo canary; self-hosted SSO isolation in v2.9.18.** Two enterprise signals in the same release cycle. Turbo is reaching into enterprise certificate requirements AND isolating self-hosted deployments from Vercel's SSO. Suggests Turbo is actively selling to enterprise self-hosted accounts.
7. **Gradle 9.6.0 config cache marketing completed.** RC1 to final in 23 days (May 28 - June 20). The CI-specific property-tracking fix is now stable and marketable. Gradle's config-cache marketing wave (4 touchpoints since April: blog, 9.5 headline, RC1, 9.6 final) is now complete. JVM-shop prospects have been primed. Nx needs comparable numbers.
8. **Rust is the default for performance-critical internals.** Pants moving PathGlobs to Rust (June 12) joins Moon's native file hashing (experiment), Turbo's Rust core, and Nx's native bindings. The entire build-tool ecosystem is converging on Rust for I/O-bound primitives.

---

## Competitive Gap Analysis (updated June 22)

| Nx Feature / Moat | Competitor Status | Gap Direction | Urgency |
|-------------------|-------------------|---------------|---------|
| **Nx Console (IDE extension)** | Turbo VS Code extension shipped (v2.9.11, May 8); web UI removed (v2.9.18) | Nx Console ahead -- Turbo scope contracted | Medium |
| **Remote caching** | All three JS competitors (Turbo, Moon, Vite+) have remote cache | Contested | Low |
| **Cache security (path traversal / manifest poisoning)** | Turbo (7 fixes, June 10); Moon (manifest path traversal, June 10) | **Both competitors patched -- Nx should audit same classes** | High |
| **Local CAS cache** | Moon (experiment, v2.3.0; stabilizing through v2.3.4) | **Moon moving ahead** | Medium |
| **MCP server (AI tooling)** | Moon (5 tools, v2.3.0); Turbo (none known) | **Moon ahead on MCP depth** | Medium |
| **Generator / scaffold discoverability via MCP** | Moon `get_template`/`get_templates` (v2.3.0) | Moon ahead | Medium |
| **Native file hashing perf** | Moon claims 10-50% (experiment) | Needs benchmark comparison | Low-Medium |
| **Daemon-based architecture** | Moon (v2.3 series hardening); Turbo (removed) | Nx/Moon aligned; Turbo diverged | Low |
| **Polyglot (Go, Python, etc.)** | Moon strong; Bazel strongest | Nx behind; Nx JVM plugin is partial | Low |
| **Affected / selective task running** | Turbo `--affected`, Moon `moon check`, all tools | Contested -- table stakes | Low |
| **`pnpm minimum release age` default** | Turbo (v2.9.15), Nx | Parity -- both have it | None |
| **Distributed task execution (DTE)** | No competitor has a direct equivalent | **Nx ahead** -- moat | Low |
| **Nx Cloud managed CI** | Turbo `experimentalCI` hint; no shipped equivalent | **Nx ahead** | Low (watch) |
| **Enterprise self-hosted isolation** | Turbo blocked self-hosted SSO from Vercel (v2.9.18) | Turbo actively hardening self-hosted | Watch |

### Where Nx Is Behind

- **Local CAS cache:** Moon's CAS experiment (v2.3.0, June 1) is stabilizing -- v2.3.3/2.3.4 hardened it without reverting it. If v2.4 promotes it to stable, Nx will need a response or a counter-narrative.
- **MCP depth for AI agents:** Moon's MCP server covers task running, project sync, affected detection, and template discovery (5 tools total). Nx generators are not yet MCP-queryable with schema introspection. Gap is widening.
- **Cache security audit:** Two competitors patched cache path traversal and manifest manipulation on June 10. Nx has not publicly confirmed auditing the same classes. This should be prioritized.

### Where Nx Is Ahead

- **Distributed Task Execution:** No competitor ships DTE. Turbo's `experimentalCI` is now 14 weeks in canary with no stable.
- **Nx Cloud:** Managed CI orchestration with cache, security controls, and observability. Turbo has remote cache; Moon has no cloud product.
- **Nx Console maturity:** Turbo contracted scope by removing web UI mode (June 10). Nx Console remains the richer IDE-integrated experience.
- **Generator ecosystem:** Nx generators (create-nx-workspace, @nx/* plugin generators) are far more extensive than Moon's templates. The gap is discovery, not breadth.

### Unassailable Moats

- **DTE + Nx Cloud:** Architectural decision requiring distributed coordination infrastructure. Moon cannot replicate with a daemon-level change; Turbo's `experimentalCI` is still undocumented at week 14.
- **Workspace generator ecosystem breadth:** Decade of community plugins and official generators. Moon templates are much fewer.

---

## Recommended Actions (updated June 22)

**High urgency:**

1. **Audit Nx cache implementation for path traversal and manifest manipulation.** Two competitors (Turbo v2.9.18 and Moon v2.3.3) patched cache security vulnerabilities on June 10. Classes: (a) remote cache manifest poisoning -> arbitrary file writes; (b) cache archive symlink escape; (c) special mode bit preservation; (d) incremental cache output containment. Nx's local and remote cache should be audited for the same. 1-2 day security audit.
2. **Assess MCP generator discoverability gap.** Moon's `get_template` / `get_templates` tools target the AI code-generation workflow directly. Nx generators are not currently MCP-queryable with schema. This is a concrete feature gap that AI-native developers will notice. Investigate whether Nx Console's MCP integration or an Nx MCP server can expose `list_generators` + `describe_generator` tools.

**Medium urgency:**

3. **Track Moon's CAS local cache experiment.** v2.3.0 shipped June 1; v2.3.3/2.3.4 have not reverted it -- it's stabilizing. If v2.4 (expected late July) promotes it to stable, the "local/remote cache format unification" claim becomes a marketing angle Moon can own. Prepare response now: either (a) ship CAS-format local cache in Nx, or (b) articulate why Nx's model is preferable.
4. **Have a native file hashing benchmark ready.** Moon claims 10-50% faster file hashing via native implementation. Nx uses Rust-native hashing via the Nx daemon. Run a head-to-head on a 50k-file repository. If Nx is already faster, publish the number.
5. **Audit Nx Cloud OAuth callback** for GHSA-hcf7-66rw-9f5r class of bug (carryover). Turbo's auth HTTP timeouts in v2.9.17-canary.2 suggest they are still hardening the OAuth path. 30-minute spot check.
6. **Audit Nx git CLI calls for argument injection.** Turbo's v2.9.18 (#13055) fixed git argument injection in SCM refs. Nx's `--base`/`--head` branch argument handling and any git CLI calls that incorporate user-supplied values should be audited for the same class of bug.

**Low urgency / monitoring:**

7. **Watch for Turbo `experimentalCI` docs.** 14 weeks in canary with no public docs. When it surfaces, assess against DTE/Nx Cloud.
8. **Watch for Moon v2.4.** v2.3 shipped June 1; v2.3 series is stabilizing through patch releases. v2.4 likely 4-6 weeks out (late July). Watch for CAS cache promotion to stable.
9. **Watch for Turbo 3.0 RC.** March narrative now 14 weeks stale. If no RC by end of July, likely rebased on a v2.10 milestone instead.
10. **Nx Console vs Turbo VS Code extension status.** Turbo removed web UI mode in v2.9.18 (a scope contraction). Nx Console advantage widened this cycle. Reassess quarterly.

---

## Competitive Landscape Summary (as of June 22, 2026)

| Tool | Version | Threat | Key Move This Cycle |
|------|---------|--------|---------------------|
| Turborepo | **2.9.18** (June 10) / canary 2.9.19-canary.9 | **HIGH (active)** | v2.9.18 (June 10): **7 security hardening fixes, web UI mode removed**. No 2.10/3.0 (14 weeks stale). Canaries ongoing. |
| Moon | **2.3.4** (June 18) | **HIGH (sustained)** | v2.3.3 (June 10): **remote cache manifest path traversal patched**. v2.3.4: toolchain maintenance. v2.3.0's CAS/MCP/hashing remain the cycle headlines. |
| Bazel | 9.1.1 + 8.7.0 LTS | Low | No new release since 9.1.1 (June 3). |
| Gradle | **9.6.0 FINAL** (June 20) | Low-Medium | **9.6.0 final shipped June 20**: config cache CI property-tracking fix now stable. `--non-interactive`, NO_COLOR, Gradle 10 deprecation preview. Marketing wave complete. |
| Pants | 2.32.0 final + 2.33.0.dev3 | Very Low | v2.33.0.dev3 (June 12): PathGlobs + Filespec moved to Rust for performance. No JS/TS crossover. |
