- CPU/memory tracking
  - OOM errors should 
    - Show CTA for cpu/memory tracking on CIPE and github comment
      - Admins, just enable and try
      - Everyone else, tell them to ask admin
  - Slow pipelines
    - Maybe use historical data for specific tasks that got slower
    - If a task got 4x slower, it might be due to memory, can we let users know?
    - Surface in CIPE maybe github comment

- Sandboxing
  - Settings should be shown to everyone
  - Overview page has actionable call outs (similar to socket.dev security analysis)
    - People who has it enabled should show if something needs attention
    - People who don't have it enabled, a message under ___ % cache hit rate to indicate
  - Security angle with cache poisoning
  - Have a trial so people can see the feature, understand how it fits into their org and make it sticky

- Others
  - There should be some call outs when something is off e.g. if utilization isn't optimized
